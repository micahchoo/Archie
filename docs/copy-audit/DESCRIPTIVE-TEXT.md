# Descriptive-text copy audit — before/after

Prose-level rewrites of explanatory / teaching / guidance copy, judged as a
first-time user who never saw Archie's design. Short labels / CTAs / terse
errors were handled in a prior pass and are not re-litigated here.

## Viewer — reader-facing surfaces

| File | Before | After | Why |
|------|--------|-------|-----|
| apps/viewer/src/components/EmptyHall.svelte | "You followed a link into a library that isn't open here. Open its `.archie.zip` file to continue." | "That link points to an exhibit in a library that isn't open here yet. Open the library file you were given to pick up where the link leads." | A pure visitor who clicked a share link needs to know WHY they landed on a blank screen and WHAT to do. "into a library that isn't open here" was builder-framed; new copy names the visitor's situation (link → exhibit → library not loaded) and the concrete action (open the file they were given). |
| apps/viewer/src/components/EmptyHall.svelte | "Open the library's `.archie.zip` file to read its exhibits. Drag it onto the page or use the button." | "Drop a library file here, or choose one with the button, to start reading its exhibits. Library files end in `.archie.zip`." | Leads with the action and the outcome (start reading) instead of repeating the H1 ("Open a library"). Keeps the `.archie.zip` handle a recipient genuinely needs, but demotes the file-extension jargon to a trailing clarifier rather than the subject of the sentence. |
| apps/viewer/src/components/NarrativeReader.svelte | "Read down the page — the view moves to each section's focus, across the items." | "Read down the page, or jump to any section. The image follows along, zooming to what each section is about, and switching between items as you go." | "the view moves to each section's focus" leaks builder terms ("the view", "focus" = the framed region). Rewrote to what the reader sees and can do: the image follows the prose, zooms to the relevant spot, and swaps items. Adds the discoverable affordance (you can also jump to a section). |
| apps/viewer/src/components/MediaPlayer.svelte | "Read down the transcript and the recording moves to each line. The line now playing is highlighted." | "Select any line to jump there in the recording. As it plays, the line being spoken lights up." | The old phrasing implied the recording auto-tracks your reading; the real interaction is the reverse — clicking a line seeks the audio/video there. Leads with what the reader does, then describes the playback feedback in plain words. |
| apps/viewer/src/components/ViewerShell.svelte | title="This site is being served from a different web address than it was published for, so share links and previews won't work. Re-publish from the address shown." | title="This site is loading from a different web address than the one it was published for, so share links and link previews won't work. The exhibits still read fine here; whoever published this site can fix the links by publishing again from its current address." | This badge currently renders for everyone, including pure visitors. Old copy commanded "Re-publish from the address shown" (no address is shown, and a visitor can't re-publish). New copy reassures the visitor the exhibits still work, then directs the fix at whoever published the site — splitting the two audiences instead of barking a developer instruction at a reader. |
</content>
</invoke>
## Overview / AV / Map / Readings / CmdK (Studio authoring surfaces)

| File | Before | After | Why |
|---|---|---|---|
| ExhibitOverview.svelte | How visitors read this exhibit (reading intent) | Choose how visitors move through this exhibit | Dropped the coined term "reading intent"; states the user's outcome plainly. |
| ExhibitOverview.svelte | Drag an object to set the reading order | Drag a media item to set the order visitors see it in | "object" → gated "media item"; spelled out what "reading order" controls. |
| ExhibitOverview.svelte | Click an object to add notes up close | Click a media item to open it and add notes | "object" → "media item"; "up close" was vague filler — say what the click does. |
| ExhibitOverview.svelte | Drag a row by its ⠿ handle to set the reading order. | Drag a row by its ⠿ handle to set the order visitors see it in. | Same plain-language gloss of "reading order" as the canvas legend. |
| AvEditor.svelte | Pause the video, then drag a box on it to mark where on the frame — it pairs with the moment in time. | Pause the video, then drag a box to point at a spot on the picture. The note covers that spot at this moment. | Replaced builder-voiced "mark where on the frame" / "pairs with the moment in time" with concrete user outcome. |
| AvEditor.svelte | Mark the start of this moment — then "Add note" ends it at the current time. Skip it to note just the current moment. | Set where the note begins. The note runs from here to wherever you are when you add it. Skip this to note a single moment. | Clearer cause/effect; "the start of this moment" was circular. |
| AvEditor.svelte | Add a WebVTT or SRT caption file — each line becomes a timed note | Add a caption file (.vtt or .srt) — each caption line becomes a note at its own time | Demoted file-format jargon (WebVTT/SRT) to a parenthetical extension hint; clarified "timed note". |
| AddMapModal.svelte | Region shown — the map is bounded to this | Area shown — visitors can only pan within this | "bounded to this" was builder-voiced; states the visitor-facing consequence. |
| AddMapModal.svelte | Set the region to exactly what's shown in the locator now | Set the area to exactly what the map below shows right now | Removed undefined coined word "locator" → "the map below". |
| AddMapModal.svelte | Centre & zoom the locator on the region | Recentre the map below so the whole area fits in view | Same "locator" removal; describes the result, not the mechanism. |
| AddMapModal.svelte | Drag the box (or its corner handles) to clamp · drag the map to pan · wheel or ± to zoom · or type exact bounds. | Drag the box (or its corners) to resize the area · drag the map to move it · scroll or ± to zoom · or type exact edges below. | Dropped jargon "clamp"/"pan"/"bounds" for plain verbs a first-timer reads. |
| ReadingsRail.svelte | New notes go into "{name}", whatever is currently visible. | File new notes under "{name}". This is where every note you draw goes, no matter which readings are shown. | "whatever is currently visible" read as a dangling fragment; rewritten to state the rule clearly. |
| ReadingsEditor.svelte | New reading — e.g. Cipher | Name a reading — e.g. Conservation, Symbolism | "Cipher" is cryptic for a first-timer; concrete curatorial examples teach what a reading is. |
| CmdK.svelte | the reference auto-resolves to its published URL when you publish | the link points to the right place once you publish | Dropped "reference auto-resolves to its published URL" jargon for a plain promise of the outcome. |

## Publish & sharing flow (Publish.svelte, PublishDialog.svelte, MergeReview.svelte, RightsEditor.svelte)

| File | Before | After | Why |
|---|---|---|---|
| MergeReview.svelte | Synced {n} notes from a colleague. | Added {n} notes from a colleague's copy. | "Synced" implies a live connection; the author opened a returned file. "Added … from a colleague's copy" matches what actually happened. |
| PublishDialog.svelte | They open your zip in their Studio, annotate their pass, and send it back — opening their copy here shows who added what. | They open your zip in their Studio, add their own notes, and send the file back — when you open their copy here, Archie shows who added what. | "annotate their pass" used the flagged coined term "pass" and builder voice; "add their own notes" is plain. Names the file round-trip explicitly. |
| PublishDialog.svelte | A shared link keeps working only while both stay online: where you uploaded the zip, and the Archie viewer at {host}. Move or remove either one and the link breaks. For a durable, citable publication, publish the full site instead. | This link depends on two things staying online: the place you uploaded the zip, and the Archie viewer at {host}. If either moves or goes away, the link stops working — so it's best for sharing a draft, not for a permanent citation. To publish something that stands on its own, use "To GitHub Pages" instead. | Leads with the dependency in plain terms, states the consequence, and points at the actual labeled choice ("To GitHub Pages") rather than the vague "the full site". |
| PublishDialog.svelte | The same published site — choose where it goes. | Archie builds the same finished site each way — pick where to put it. | Cryptic for a first-timer; clarifies that every option produces the same result and the choice is only the destination. |
| PublishDialog.svelte | Choose the folder the Viewer reads — the one fixed target: … Pick it once; re-publish any time (stale files are cleaned). | Choose the folder the Viewer reads from: … Pick it once, then re-publish any time — Archie clears out old files for you. | "the one fixed target" is builder phrasing; "stale files are cleaned" passive. Reframed as a user benefit. |
| PublishDialog.svelte | Then visit http://localhost:4321. Re-publish any time — the write is idempotent. | Then visit http://localhost:4321. Re-publish any time — it safely replaces what's there. | "idempotent" is dev jargon a curator won't know; plain restatement of the same guarantee. |
| PublishDialog.svelte | Your browser can't pick a folder, so this saves a .archie.zip. You'll then unzip it into the Viewer's one fixed folder — instructions next. | Your browser can't pick a folder, so this saves a .archie.zip instead. You'll then unzip it into the folder the Viewer reads from — instructions next. | "the Viewer's one fixed folder" is builder phrasing; reworded to plain language consistent with the other folder copy. |
| Publish.svelte | Pushed to the {branch} branch. To serve it on the web, turn on GitHub Pages for this repo: | Your files are on the {branch} branch. One step left to put them on the web: turn on GitHub Pages for this repository. | "Pushed to the branch" is dev voice; reframed as the user's progress with a clear "one step left" for the non-developer. |
| Publish.svelte | The token never leaves this browser except in the publish request to GitHub, and is dropped the moment publishing finishes. Archie does not store it. Pages write-access lets Archie turn on GitHub Pages for you; without it the push still works and you enable Pages by hand. | Your token stays in this browser — it's sent only to GitHub to publish, then dropped the moment it's done. Archie never stores it. Giving the token Pages write access lets Archie switch on your live site for you; without it, publishing still works and you flip the switch yourself (we'll show you where). | Keeps the necessary token-scope specifics but frames them for a non-developer; "your live site" instead of restating "GitHub Pages"; "we'll show you where" reassures since the walkthrough does follow. |
| Publish.svelte | A note or exhibit a link points to isn't in this library, so the link can't resolve. Publishing continues — the link's words stay, the link drops. | These links point to a note or exhibit that isn't in this library, so they have nowhere to go. Publishing continues — the words stay readable, just without the link. | "can't resolve" / "the link drops" leak technical phrasing; reworded so a curator understands the words survive, the link does not. |

## Studio editor — annotate surface (App.svelte)

| File | Before | After | Why |
|---|---|---|---|
| App.svelte | Drop pins on a live map — each anchored to a longitude/latitude that stays put as you zoom and pan. A prototype of Archie's geo-annotation extension. | Drop pins on a live map — each one stays on its place as you zoom and pan, anchored to a longitude and latitude. An early look at annotating maps in Archie. | "geo-annotation extension" is builder vocabulary describing the system, not the user's outcome. Reframed to what the curator does (annotating maps) and "an early look" instead of "a prototype". |
| App.svelte | How visitors read this exhibit (reading intent) | Choose how visitors move through this exhibit — a grid to browse, a single view, or a guided sequence | "reading intent" is a flagged product-coined term shown with no teaching. Replaced with a plain action ("Choose how visitors move") that names the three concrete outcomes. |
| App.svelte | Best for large files: a link points to the file instead of copying it into your library — keeps your .archie.zip small. | Best for large files: a link points to the media where it already lives instead of copying it in, so your library file (.archie.zip) stays small. | Leads with the benefit, frames ".archie.zip" as "your library file (.archie.zip)" per the prior pass's keep-but-frame rule, and "the media where it already lives" reads truer than "the file". |
| App.svelte | No notes on this recording yet. Play it, then "Mark start" → "Add note" to mark a moment. | No notes on this recording yet. Press play, hit "Mark start" at the moment you mean, then "Add note" to pin one there. | Teaches the full gesture in order and keeps the literal button labels ("Mark start", "Add note") so a first-timer can find the controls; "pin one there" makes the result concrete. |
| App.svelte | Notes here are hidden — turn their readings on in the panel beside the image. | This media item has notes, but they're hidden right now. Switch a reading on in the panel beside the image to show them. | The original assumed the user already knows what a reading is and why notes vanished. States the situation (notes exist but are hidden), then the single action to reveal them; uses the canonical noun "media item". |
| App.svelte | No notes on this object yet. Start a note above — choose Box or Outline, then draw the region. | No notes on this media item yet. Start one above — pick Box or Outline, then draw the region it points to. | "object" → canonical "media item"; "the region it points to" teaches that a note points at part of the media. |
| App.svelte | Use "Mark start" on the recording to mark this section's moment. / Draw a box on the image to frame this section's camera — it isn't a note. (tag: Framing camera) | Hit "Mark start" on the recording to set where this section opens — this sets the view, not a note. / Draw a box on the image to set what this section shows when a visitor reaches it — this sets the view, not a note. (tag: Setting the view) | "camera" / "frame" / "Framing camera" are flagged coined metaphors never taught. Rewritten to plain outcome ("set what this section shows when a visitor reaches it"), matching the sibling NarrativeEditor's "Set the area / Set the moment" wording. |
| App.svelte | Columns: object, x, y, w, h, comment (tags and reading optional). First row must be headers. Object can be a name, an id, or left blank for this object. | Bring notes in from a spreadsheet. Columns: object, x, y, w, h, comment (tags and reading are optional), with a header row first. In the object column, use a media item's label — or leave it blank to use the one you're on. | Opens by saying what the feature does (the user had no lede). Drops "an id" (an internal handle the curator never sees) and uses "media item's label" + "the one you're on" for the blank-row case. |
| App.svelte | Play it · "Mark start" → "Add note" marks a moment (video: "Draw a box on the video" adds a box) · click a note to jump to it and edit. | Press play · "Mark start" then "Add note" pins a note to that moment (on video, "Draw a box on the video" points at a spot too) · click any note to jump back to it and edit. | Keeps the literal button names but replaces the terse "→ marks a moment" with the concrete result ("pins a note to that moment"); "points at a spot" explains what the video box does. |
| App.svelte | Start a new note → choose a shape → draw the region · click a marker to edit it right there · its editor follows it as you pan/zoom. | Start a new note · pick a shape · draw the region it points to · click a marker to edit that note right where it sits — its editor stays pinned to it as you pan and zoom. | "the region it points to" teaches what the drawn shape is for; "stays pinned to it as you pan and zoom" reads clearer than "follows it as you pan/zoom" for a first-timer. |

## Builder-voice prose (LayoutPicker, NarrativeEditor, ReadingsModal, LibraryHome)

| File | Before | After | Why |
|------|--------|-------|-----|
| LayoutPicker.svelte | Each choice sets a different relationship between the visitor, the work, and your voice — not a skin on the same page. It shapes the published exhibit, not this editing view. | Each option changes how visitors move through the published exhibit — what they see first and how your writing fits around it. This only affects the published exhibit, not how you edit it here. | "a relationship between the visitor, the work, and your voice" and "skin on the same page" are builder metaphors a first-timer can't decode; replaced with concrete outcomes (what visitors see first, where your writing fits). |
| LayoutPicker.svelte | Single — Hold the visitor on a single object. / It fills the view; your notes read alongside it. | Single — Keep visitors on one media item. / It fills the view, with your notes alongside it. | "object" violates the media-item terminology gate; light tidy of the consequence sentence. |
| LayoutPicker.svelte | Grid — Set out a collection to wander. / Visitors browse the objects and choose where to look. | Grid — Lay out the whole collection to browse. / Visitors scan the media and pick what to look at. | "object" → "media"; "wander/browse" simplified to plain action a first-timer follows. |
| LayoutPicker.svelte | Narrative — Lead the reading with your own prose. / Your sections carry the object to each passage, in the order you set. | Narrative — Let your writing lead the way through. / Your sections move from one media item to the next, in the order you set. | "carry the object to each passage" is builder phrasing; "prose"→"writing"; "object"→"media item"; teaches what a Section does plainly. |
| LayoutPicker.svelte | Object-led — the work is what they came for | Media first — the media is what visitors came to see | "Object-led" / "the work" are coined/metaphorical; relabeled to plain "Media first" with a clause a visitor-curator follows. |
| LayoutPicker.svelte | Prose-led — your writing leads; the work follows | Writing first — your writing leads and the media follows along | "Prose-led" / "the work" replaced with "Writing first" and "the media"; plain language, no internal axis vocab. |
| LayoutPicker.svelte | Later: Compare (two objects side by side) joins here as a new arrangement; Slideshow paces a Grid — as a variant, not a new template. | More arrangements are coming for this group, such as comparing two items side by side. | Roadmap/builder note ("variant, not a new template", "objects") rewritten as a short plain "coming soon" line about what the user gains. |
| LayoutPicker.svelte | Later: Scrollytelling joins here — the same prose reading, paced by scroll rather than click. | More ways to pace your writing are coming for this group, such as advancing it by scroll. | "Scrollytelling" / "prose reading" are jargon; reframed to a plain coming-soon benefit. |
| LayoutPicker.svelte | (tooltip) How v1.1 reading modes attach to this family | (tooltip) Coming soon to this group | "v1.1 reading modes" / "family" are internal version + axis vocab; plain restatement. |
| LayoutPicker.svelte | Narrative reads its prose from the exhibit's sections — author them in the spine panel beside your notes. | Narrative is built from your sections — short passages you write, each paired with a media item. Add and order them in the narrative panel beside your notes. | "reads its prose", "spine panel", "author them" are builder voice; teaches what a Section is in plain words and names the panel plainly. |
| NarrativeEditor.svelte | The reading spine — beats shown in order, each framed on one object. It spans the whole exhibit and stays as you move between objects. | A narrative is a sequence of sections — short passages you write, each shown with one media item. It runs through the whole exhibit and stays put as you switch between media items. | "reading spine", "beats", "framed on one object" are coined metaphors; teaches Narrative + Section on first use; "object"→"media item". |
| NarrativeEditor.svelte | Add an object to the exhibit first — a section is framed on one. | Add a media item to the exhibit first — every section is shown with one. | "object" + "framed on" replaced with plain language. |
| NarrativeEditor.svelte | On this object · {label} / On · {label} | Shown with this item · {label} / Shown with · {label} | "object" terminology; reworded to plainly say where the section appears. |
| NarrativeEditor.svelte | (tooltip) Attach this section to the object you're viewing (clears its framed view) | (tooltip) Show this section with the media item you're viewing now (clears the area it currently highlights) | "object" + "framed view" camera metaphor; plain restatement of the consequence. |
| NarrativeEditor.svelte | (placeholder) Prose for this beat… | (placeholder) Write this section… | "beat" is coined; plain instruction. |
| NarrativeEditor.svelte | Framing… set the moment on the recording / Framing… draw a box on the image | Set the moment — scrub to it on the recording / Set the area — draw a box on the image | "Framing…" camera metaphor replaced with a plain gesture instruction that still tells the user exactly what to do. |
| NarrativeEditor.svelte | No camera framed | Whole item shown | "camera framed" metaphor; states the actual default (no region set = the whole item shows). |
| NarrativeEditor.svelte | Reframe / Frame camera / Set moment (buttons) | Change view / Set area / Set moment | "Reframe" / "Frame camera" camera metaphors replaced with plain verbs; "Set moment" (AV) kept as already plain. |
| NarrativeEditor.svelte | framed region (camera-state label) | area set | Removes the "framed" metaphor from the visible state label. |
| ReadingsModal.svelte | Ways of reading this source | Ways of reading this exhibit | Readings span the whole exhibit, not a single "source"; aligns heading with what's actually scoped. |
| ReadingsModal.svelte | A reading is one way of interpreting this source — a Cipher reading beside a Hoax reading. Rival readings sit side by side; they are never merged. Name, colour, and describe each one here. | A reading is one interpretation of what's in this exhibit — for example, a Cipher reading beside a Hoax reading. Competing readings sit side by side and are never merged. Name, colour, and describe each one here. | Already teaches "reading" well; tightened — "this source"→"this exhibit", "Rival"→"Competing" (plainer), kept the concrete Cipher/Hoax example. |
| LibraryHome.svelte | Exhibits marked Example are templates — open one to explore (nothing's saved), keep a copy to make it yours. Your own exhibits save as you work. Start a new one any time. | Exhibits marked Example are ready-made samples to explore — changes you make to them aren't kept, so use "Keep a copy" to turn one into your own. Exhibits you create save as you work. Start a new one any time. | "templates" + cramped "(nothing's saved)" clarified for a first-timer; quotes the real "Keep a copy" button so the path is discoverable; still warm and accurate. |

## Verification-pass straggler (ExhibitOverview layout-intent sub-label)

| File | Before | After | Why |
|---|---|---|---|
| ExhibitOverview.svelte | one object, full attention / a wall of objects to scan / a guided sequence with prose | one media item, full attention / a wall of media to browse / a guided sequence led by your writing | The `LAYOUT_INTENT` sub-label rendered in the overview header still said "object"/"objects" (terminology gate: container item is "media item" / collective "media") — missed by the earlier sweep because no audit row covered these three values. Aligned to the gated nouns and to LayoutPicker's plain stance voice ("browse", "led by your writing"). |
