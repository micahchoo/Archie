# Copy audit — bucket: studio-dialogs-flows

Audited 2026-06-17. Paths relative to repo root `/mnt/Ghar/2TA/DevStuff/Annotators/Image/Archie`.

Files in scope:
- apps/studio/src/Publish.svelte
- apps/studio/src/PublishDialog.svelte
- apps/studio/src/MergeReview.svelte
- apps/studio/src/IdentityPrompt.svelte
- apps/studio/src/publish-flows.svelte.ts
- apps/studio/src/collab.ts
- apps/studio/src/save-queue.svelte.ts
- apps/studio/src/store.ts (no user-visible runtime copy)
- apps/studio/src/shortcuts.ts
- apps/studio/src/library-meta-reducers.ts (no user-visible runtime copy)
- apps/studio/src/bake.ts (only dev-facing thrown errors, caught into a failure count upstream — treated as out of scope)
- apps/studio/src/iiif-import.ts
- apps/studio/src/wadm-import.ts
- apps/studio/src/csv-import.ts
- apps/studio/src/folder-import.ts

Note on surfacing: the `skipped[].reason` strings in csv-import.ts and wadm-import.ts and the `ManifestImportError` messages in iiif-import.ts ARE user-visible — App.svelte concatenates the reasons into the `importNote` toast (App.svelte:620-621, 647-648) and alerts the manifest error (App.svelte:571). The save-queue `label didn't save` string surfaces in the save-state chrome. `collabSummaryText` is shown as `collabNote`.

## Fixes

| # | File | Line | Current (verbatim) | Issue | Proposed | Severity | Category |
|---|------|------|--------------------|-------|----------|----------|----------|
| 1 | apps/studio/src/PublishDialog.svelte | 146 | `A <code>?src=</code> link works while both hosts stay up — the zip's and <code>{CANONICAL_HOST}</code> (the Viewer). Moving either breaks shared links.` | Leaks raw query-param token `?src=` to the user; em-dash + parenthetical in a dense line | Rework without the literal `?src=`, e.g. "A shared link keeps working only while both stay online: where you uploaded the zip, and the Archie viewer at {CANONICAL_HOST}. Move or remove either one and the link breaks." | medium | internal-vocab |
| 2 | apps/studio/src/PublishDialog.svelte | 139-140 | `{copied ? "Copied ✓" : "Copy link"}` / `{copiedEmbed ? "Copied ✓" : "Copy embed code"}` | Checkmark glyph used as forced-cheer confirmation flourish | "Copied" (drop the ✓; the label change is the confirmation) | low | oops-cheer |
| 3 | apps/studio/src/PublishDialog.svelte | 129 | `Saved your <code>.archie.zip</code>.` | "your" is slightly chummy filler vs the sibling "done-zip" state which says `Saved {zipName}.` — inconsistent voice between two adjacent success states | "Saved your library as a .archie.zip." (or match the sibling: "Saved your copy.") — pick one and make the two done states consistent | low | inconsistency |
| 4 | apps/studio/src/PublishDialog.svelte | 97 | `<h2>Where to?</h2>` | Clever-terse heading; doesn't say what is being placed | "Where should this go?" or "Choose where to publish" | low | vague-cta |
| 5 | apps/studio/src/PublishDialog.svelte | 134, 154, 163, 165 | Share link / `pnpm --filter @archie/viewer dev` / `{viewerTree}` shown in `<pre class="cmd">` | Surfacing a raw monorepo pnpm command and the internal tree path (`apps/viewer/public/published/`) to authors — heavy dev/internal vocabulary. May be unavoidable for the local-dev flow, but flag for review: a non-technical author can't act on `pnpm --filter`. | Confirm the audience: if authors are expected to run a terminal this is acceptable; otherwise gate the command behind a "for developers" disclosure and lead non-technical authors to the hosted/zip path. | medium | internal-vocab |
| 6 | apps/studio/src/Publish.svelte | 111 | `· note <code>{(tgt(b).noteLogicalId ?? "").slice(0, 8)}</code>` | Shows a truncated `logicalId` hash to the user as a note identifier — raw internal id, meaningless to an author | Identify the note by something human (its text snippet or the exhibit it sits in) rather than an 8-char id; if no human handle exists, drop the id and say "a cited note" | medium | internal-vocab |
| 7 | apps/studio/src/Publish.svelte | 107 | `⚠ {brokenLinks.length} cited {brokenLinks.length === 1 ? "link" : "links"} will publish as plain text` | Leading `⚠` emoji/glyph as decoration (repeats across 121, 130) | Drop the `⚠` glyph; the warning panel styling already signals it. Keep the text. | low | oops-cheer |
| 8 | apps/studio/src/Publish.svelte | 124 | `Access token (fine-grained PAT · Contents + Pages, write)` | "PAT" abbreviation + middot styling; "fine-grained PAT" is GitHub jargon dense for a field label | "Access token (fine-grained, with Contents and Pages write access)" | medium | internal-vocab |
| 9 | apps/studio/src/Publish.svelte | 67 | `"Publish failed — check the repo name and token scope."` | "token scope" is GitHub/OAuth jargon; em-dash in a short error | "Couldn't publish. Check the repository name and that your token has Contents and Pages write access." | medium | error-quality |
| 10 | apps/studio/src/Publish.svelte | 92 | `<p class="ok">Published.</p>` | Bare one-word success; fine, but the sibling PublishDialog success states are fuller ("Published locally.", "Saved ...") — slightly terse/inconsistent | "Published to GitHub Pages." | low | inconsistency |
| 11 | apps/studio/src/Publish.svelte | 87 | `Publish this whole library — every exhibit — to a GitHub Pages branch.` | Double em-dash aside in a lede (punctuation tell in short UI copy) | "Publish your whole library — every exhibit — to a GitHub Pages branch." → prefer commas: "Publish your whole library, every exhibit, to a GitHub Pages branch." | low | punctuation |
| 12 | apps/studio/src/Publish.svelte | 98 | `<a ...>Settings → Pages</a> → <em>Deploy from a branch</em> → <code>{branch}</code>. It then appears at ...` | Chained `→` arrows as instructional connective — directional/glyph-as-syntax, hard to read aloud | "Open Settings, then Pages, choose Deploy from a branch, and pick the {branch} branch. Your site then appears at ..." | low | directional |
| 13 | apps/studio/src/Publish.svelte | 100 | `For editing and the fullest experience, run the apps locally — a published Pages site is read-only.` | "run the apps locally" leaks the dev model; "fullest experience" is vague marketing | "A published Pages site is read-only. To keep editing, open your library in Studio." | low | vague-cta |
| 14 | apps/studio/src/MergeReview.svelte | 36 | `Synced {synced} {synced === 1 ? "note" : "notes"} from a colleague.` ... `{conflicts.length} {conflicts.length === 1 ? "needs" : "need"} your decision.` | "Synced" implies live server sync, but this is opening a returned zip — verify the verb matches the actual mental model (the team's model is zip exchange, not live sync) | Confirm whether "Synced" is the intended word vs "Merged"/"Brought in"; the rest reads well | low | terminology |
| 15 | apps/studio/src/MergeReview.svelte | 39 | `<button class="ghost" onclick={() => (synced = 0)}>Later</button>` | "Later" is a slightly vague CTA for "dismiss this banner for now" | "Not now" or "Review later" | low | vague-cta |
| 16 | apps/studio/src/MergeReview.svelte | 23 | `return ... ?? "(empty)";` | `(empty)` shown as the body of a conflict side when a note has no text — parenthetical placeholder-ish; acceptable but could read clearer | "No text" (or "(no text)") to read as a state, not a null placeholder | low | placeholder |
| 17 | apps/studio/src/MergeReview.svelte | 45 | `<p class="eyebrow">Resolve · {conflicts.length} left</p>` | Middot separator in a short label (punctuation tell) | "Resolve — {conflicts.length} left" reads no better; prefer "{conflicts.length} left to resolve" | low | punctuation |
| 18 | apps/studio/src/IdentityPrompt.svelte | 32 | `<button type="button" class="ghost" onclick={onskip}>Skip — stay Anonymous</button>` | "Anonymous" capitalized as if a proper term (it is the internal fallback editor name); em-dash in a button label | "Skip for now" (the "stays anonymous" nuance belongs in body copy, not the button); if keeping it, lowercase: "Skip — stay anonymous" | low | internal-vocab |
| 19 | apps/studio/src/collab.ts | 37 | `Opened "{name}" — it carries {who}{yours}. Annotate your pass and send the zip back to keep the exchange going.` | "Annotate your pass" leans on the internal "Reading"-as-pass concept; "{who}" expands to "N notes by {editor}" where editor may be the literal "unknown" fallback (collab.ts:20) | Confirm "pass" reading; and ensure the editor name never renders the literal "unknown" to a user (substitute "a collaborator"). Otherwise the line is good. | medium | terminology |
| 20 | apps/studio/src/save-queue.svelte.ts | 57 | `` `${label} didn't save: ${err instanceof Error ? err.message : String(err)}` `` | Appends a raw `err.message` (which can be a low-level DOMException / OPFS string) to user copy — risk of leaking internal error text | Keep the friendly prefix; map known failures to plain text and only append the raw message when no mapping exists, e.g. "{label} couldn't be saved. {friendly cause}." | medium | error-quality |
| 21 | apps/studio/src/shortcuts.ts | 20 | `{ keys: "⌘K", label: "Cite a note or exhibit (while editing a note or section)", group: "Image" }` | Grouped under "Image" but the action is general (note/section) — minor group mismatch; copy itself fine | Consider group "Anywhere" if it fires outside image context; copy is fine | low | inconsistency |
| 22 | apps/studio/src/iiif-import.ts | 92 | `"That's a IIIF Collection (a list of manifests). Paste the URL of one of its manifests — collection import is on the roadmap."` | "IIIF Collection"/"manifests" is correct IIIF domain language for this audience (importing IIIF), so acceptable; but "on the roadmap" is product-speak in an error | "This is a IIIF Collection (a list of manifests). Paste the URL of a single manifest instead." (drop "on the roadmap" or move to a quieter note) | low | error-quality |
| 23 | apps/studio/src/iiif-import.ts | 86, 97 | `"That URL didn't return a IIIF manifest."` | Good plain error, no blame; just verify it's specific enough — same string for two different failures (not an object / not a manifest shape). Acceptable. | (no change) — keep | low | error-quality |
| 24 | apps/studio/src/iiif-import.ts | 119 | `"That manifest has no canvases Archie can read."` | "canvases" is raw IIIF/internal vocab; an author pasting a URL may not know what a canvas is | "That manifest has no images or media Archie can import." | medium | internal-vocab |
| 25 | apps/studio/src/iiif-import.ts | 122 | `return "Imported manifest";` (fallback exhibit title) | Fallback title "Imported manifest" leaks the internal "manifest" word as a user-visible exhibit title | "Imported exhibit" or "Untitled exhibit" (match App.svelte:488 default "Untitled exhibit") | medium | internal-vocab |
| 26 | apps/studio/src/wadm-import.ts | 99 | `"legacy oa:/sc:AnnotationList — convert to W3C Web Annotation (e.g. via Mirador export) first; native support is on the roadmap"` | Raw schema tokens (`oa:`, `sc:AnnotationList`, `W3C Web Annotation`) shown verbatim; semicolon + "on the roadmap" product-speak; this string is concatenated into a toast | "This file uses an older annotation format Archie can't read yet. Re-export it as W3C Web Annotation (for example, from Mirador) and try again." | high | internal-vocab |
| 27 | apps/studio/src/wadm-import.ts | 103 | `"no annotations found — expected a W3C AnnotationPage, an Annotation array, or one Annotation"` | `AnnotationPage`, `Annotation array` are raw schema/type names surfaced to users | "No annotations found in that file. Archie reads a W3C Web Annotation file (a single annotation or a list)." | high | internal-vocab |
| 28 | apps/studio/src/wadm-import.ts | 113 | `` `target ${JSON.stringify(...)...} doesn't match an object here (…/canvas/<id>)` `` | Dumps a JSON-stringified target IRI and the raw `…/canvas/<id>` path grammar into a user-visible skip reason | "Points to media that isn't in this exhibit." (drop the IRI dump and the canvas-path grammar) | high | internal-vocab |
| 29 | apps/studio/src/wadm-import.ts | 75, 81, 85 | `` `unsupported fragment "${...}"` `` / `"unsupported or unsafe SvgSelector"` / `` `unsupported selector type "${t...}"` `` / 71 `"annotation has no selector"` / 123 `"no usable TextualBody"` | "fragment", "SvgSelector", "selector type", "TextualBody" are raw WADM/code type names shown in the skip-reason toast | Translate to plain region/text terms, e.g. "unsupported region shape", "the region is missing or unreadable", "no usable note text". Verify each against what an author can understand. | high | internal-vocab |
| 30 | apps/studio/src/csv-import.ts | 94 | `"AV object — regions are image-only"` | "AV object" is internal shorthand; em-dash in a terse reason | "Audio and video can't take a region — skipped." | medium | internal-vocab |
| 31 | apps/studio/src/csv-import.ts | 76 | `` `missing column(s): ${missing.join(", ")} — expected a header row like "object,x,y,w,h,comment"` `` | Good, specific, shows the fix; only "column(s)" parenthetical-plural is a minor polish tell | "Missing columns: {list}. The first row must be a header like: object,x,y,w,h,comment" | low | redundant |
| 32 | apps/studio/src/csv-import.ts | 111 | `` `unknown reading "${readingRaw}" — create it first` `` | Uses glossary term "reading" correctly; "create it first" assumes the author knows where. Good, minor. | "no reading named \"{readingRaw}\" — add it to this exhibit first" | low | terminology |
| 33 | apps/studio/src/csv-import.ts | 99-100 | `"x,y,w,h must all be numbers with positive w,h"` | Column-letter shorthand is correct (matches the documented dialect) but reads code-ish; acceptable since it mirrors the header | "x, y, w, h must all be numbers, and w and h must be greater than zero." | low | error-quality |
| 34 | apps/studio/src/folder-import.ts | 51 | `return root.trim() || "Imported folder";` (fallback exhibit name) | Fallback name "Imported folder" becomes a user-visible exhibit title; acceptable but slightly generic vs the "Untitled exhibit" convention used elsewhere | Confirm desired fallback title; consider "Imported folder" → keep, or align with "Untitled exhibit" | low | inconsistency |
</content>
</invoke>
