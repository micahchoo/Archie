# HANDOFF — Voynich Exhibit Rewrite

**As of:** 2026-05-27 · prior session ended near context limit after dispatching Wave 1.

## What this is
Rewrite of Archie's broken "Voynich" demo exhibit so it showcases the product. Root problem was **content** (factually + structurally bad), so we work **content → analysis → structure → build → verify**. Full plan + locked decisions: `00-plan.md` (read it first).

## Locked decisions (do not re-litigate)
- Show off the product via a genuinely good exhibit.
- Single source of truth — kill the studio/viewer `voynich.ts` twins; viewer copy generated by publish pipeline.
- Pull in NEW real material; reorganize core content.
- Include ONE AV object (showcases AV feature).
- Versioning = studio-side demo only.

## State
- **Wave 1 (Phase 1: Content) — COMPLETE.** Both files written + well-sourced:
  - `01-manuscript-foundation.md` — factual spine + 10 real folios spanning all 6 sections, each verified 200 OK against Yale's live IIIF manifest.
  - `02-av-and-material-manifest.md` — recommended AV asset + consolidated material manifest.
- **Wave 2 (Phase 2: Analysis) — COMPLETE.** `03-analysis.md` written. Outcomes:
  - 8 readings regions (R1–R8) where cipher/hoax/abjad maximally diverge, across the folios; even-handed voice rule enforced. f68r/f75r/f99r flagged as next-author regions so the toggle is live on all image folios.
  - Tags: 10-term vocabulary mapped folio-by-folio (Currier hands = Tags, not Readings).
  - Sections: 6-beat spine (herbal→astronomical→balneological→cosmological→pharmaceutical→recipes), curator voice.
  - **DECISION: add f18v as an 11th object** (verified live: imageId `1006109`, 2846×3781, herbal) so the Kryptogramm audio plays against the page it sonifies. 4 time-anchored `t=` notes authored (`Sound` canvas per published `av` exhibit shape).
  - Cross-link 1 (recommended): Rosettes (R7) → `bidar` canvas o1 (geography-as-network angle). Link 2 deferred to Phase 3.
- **Phase 3 (Ideation) — COMPLETE.** `04-design.md` = build spec. Key resolutions:
  - **Corrected architecture** (Scout B's `sample-data.ts`/`av.ts` paths were wrong): true pipeline is seed `apps/viewer/src/sample-data.ts` → `scripts/gen-published.mts` → `public/published/<slug>/` IIIF JSON → `loadPublishedExhibit` → ExhibitView. **Author the seed, regenerate; never hand-edit published.**
  - Borked-in-code: `sample-data.ts:42` has only 2 readings (need 3, add `abjad`); `addVoynich` fires on o2/o3/o5 only (the "2-of-5"); `voynich.ts` is a stripped `VoynichObject/VoynichNote` fixture; layout=grid so the spine had no home.
  - Layout → **narrative** (Sections require it; grid was never on the showcase list).
  - 12 objects: 11 images (IIIF-direct from Yale) + 1 sound (o12, Kryptogramm).
  - A2 surgery: rewrite `apps/viewer/src/voynich.ts` as the one seed, **delete `apps/studio/src/voynich.ts`**, point App.svelte at it.
  - `[SNAG-PRESPEC]` `importTranscript` (AV) does NOT carry `reading` → Phase 4 needs a small `addAvNote` helper for the reading-bearing AV-1…4 notes.
- **Phase 4 (Build) — Wave 1 (SCAFFOLD) DISPATCHED, running in background.** Scope: §A A2 surgery (rewrite `apps/viewer/src/voynich.ts` as single seed, delete `apps/studio/src/voynich.ts`, fix App.svelte import), §B 12-object set (Yale IIIF-direct sources from the manifest + o12 sound), §C readings 2→3 (add `abjad`), §E `addAvNote` helper, §I keep `voynich-readings.test.ts` green. Must compile (viewer typecheck + render-core tests, fnm Node v24).
  - **Wave 1 — DONE & GREEN** (355/355 render-core tests; viewer gen 95 files w/ 12 canvases + abjad page; studio builds). A2 surgery complete; `apps/studio/src/voynich.ts` deleted; single seed = `apps/viewer/src/voynich.ts`. Layout left grid (deferred). o12 audio still on archive.org (re-host TODO).
  - **Wave 2 (CONTENT) — DONE & VERIFIED.** Only `apps/viewer/src/sample-data.ts` edited. 33 reading-notes (11 regions ×3) + tags; 6-beat `voynichSections`, layout→narrative; 4 AV notes (base — toggle is internal to each note's prose, not external); bidar cross-link (R7→`archie:bidar/`). Cover→IIIF derivative (verified 200 image/jpeg). Builds GREEN: render-core 355/355, viewer gen 128 files, viewer+studio build clean. **Independently confirmed on disk:** published/voynich = 12 canvases, cipher/hoax/abjad pages ×12, 6 Ranges, summary rewritten, o12 sound canvas.
  - Deviations to know: AV notes are reading-less (per `04`§E); `archie:` link grammar is `archie:slug/` (single colon), NOT `archie://`; AV-1 link resolves to voynich root (note-precise target needs a stable logicalId — Studio follow-up).
- **Phase 4 RUNTIME BUGS (user-found; build-green missed them — `[SNAG]` declared "done" on compile, not on running):**
  1. `voynich/canvas/o1/annotations.json` 404 — file exists in public/published (items~1); `published.ts:55` fetch is non-optional → likely stale served tree / base-path; needs reproduction against a running dev server.
  2. **Studio shows no notes/readings/sections** — root cause: studio seeds SEPARATELY (`App.svelte seededVoynich()` loops `voynichNotes`=`[]`; `DEFAULT_EXHIBITS` voynich has no readings/sections). All content was authored viewer-side only. Fix = extract authored content to shared `apps/viewer/src/voynich.ts`, wire BOTH apps to it; bump studio `seedVersion` to force reseed.
  3. **No rail thumbnails** — objects use bare IIIF service-base URLs; thumbnails need a derivative (`…/full/240,/0/default.jpg`). Cover was patched; rail/grid not.
- **All 3 bugs FIXED & run-verified** (7 files: viewer voynich.ts/sample-data.ts/ObjectGrid.svelte/Reader.svelte, studio App.svelte, render-core resolve.ts +test). Lesson saved: memory `verify-archie-by-running`.
  1. 404 = OPERATIONAL (stale tree). `pnpm dev` predev-hook runs `gen` → tree regenerates → o1/annotations.json now 200. No code change. **User must restart dev / re-gen to clear.**
  2. Studio: authored content extracted to shared `apps/viewer/src/voynich.ts` (`voynichReadings`/`voynichReadingNotes`(33)/`voynichAvNotes`(4)/`voynichSections`(6)); BOTH apps consume it (true single source). Studio `seededVoynich` now seeds them; `seedVersion:1` forces reseed. Verified: seed yields 37 notes (33 reading, 11 tagged). gen output byte-equivalent (ULID-only diff).
  3. Thumbnails: `thumbnailUrl()` helper in render-core `resolve.ts` (IIIF base → `/full/<w>,/0/default.jpg`); applied in viewer grid/reader + studio rail. Verified 200 image/jpeg + 5 unit tests. OSD keeps bare base.
  - render-core: **386 tests pass**; both app builds clean.
- **Screenshot capture DISPATCHED (background)** — `scripts/capture-screenshots.mjs` (Playwright) for actual visual sign-off of both apps. PNGs → `docs/screenshots/auto/`.
- **`[SNAG]` ARCHITECTURE CONFLICT found via screenshots (NOT yet fixed):** Readings and Sections are NOT co-presentable in one exhibit in the current viewer. `Reader.svelte` (grid/single) mounts `ReadingLegend` + shows reading-notes; `NarrativeReader.svelte` (narrative) does NOT — no readings props, no legend, and `activeNotes` reads only BASE annotations (our 11 folios have base=0; all content is in reading pages). So voynich-as-narrative shows the 6 sections but ZERO readings legend + ZERO of the 33 reading-notes. Studio screenshots confirm studio-side notes/sections/thumbnails DO render; this is viewer-only.
  - **THREE-EXHIBIT BUILD — DONE & VERIFIED ON SCREEN; committed at HEAD `7fdb657` (main).** single `voynich-rosettes` (Rosettes deep-zoom + 3-option legend ✓), grid `voynich` (12-folio grid + carousel 1/12 + legend + #tags ✓), narrative `voynich-reading` (6-section spine + legend + toggle marker ✓). 389 tests; viewer+studio build. Seed: `buildVoynichLog(slug,{objectIds,includeAv})` per-slug logs; studio `seededVoynich(slug,opts)`; pages voynich-rosettes.astro + voynich-reading.astro.
  - **FOLLOW-UP WAVE — DONE & VERIFIED BY RUNNING.** (1) 11 base reading-less captions o1–o11 → default/base view shows markers in all 3 exhibits (confirmed on screen). (2) Rights: per-folio Beinecke public-domain mark + AV o12 CC-BY-NC-SA 3.0, on `voynichObjects` (AObject extends RightsFields → studio too); rendered in viewer. (3) Cross-links repointed: R7→`voynich-rosettes`, AV-1→`voynich-reading`; 0 voynich→bidar/av links; brokenLinks=0. 389/389 tests; gen 255 files; both builds green.
  - Minor UX follow-up (not done, not requested): shared note bodies mean R7's link self-references on the rosettes page & AV-1's on the narrative page — still resolve validly.
  - **SUNSET bidar+av (2026-05-27): backup DONE outside repo** (`Image/archie-sunset-backup-2026-05-27/` + RESTORE.md, 41 files). **Sunset agent DISPATCHED** — removes ex-bidar/ex-av from sample-data.ts + App.svelte, deletes bidar.ts×2 + pages/{bidar,av}.astro + published/{bidar,av} + obsolete .gitignore lines; KEEPS shared AV infra (MediaPlayer/AvEditor/importTranscript/av time.test.ts — voynich-reading uses it). Verify-by-running: gallery shows ONLY 3 voynich, /bidar /av 404. (Earlier step: bidar/av published trees were gitignored+untracked.)
  - **SUNSET — DONE & VERIFIED BY RUNNING.** bidar+av removed from sample-data.ts + App.svelte; bidar.ts×2 + pages/{bidar,av}.astro + published/{bidar,av} deleted; .gitignore tidied. AV infra kept (voynich-reading uses it). Gallery shows ONLY 3 voynich; /bidar /av → 404; 395/395 tests; viewer build = 4 routes; studio green; gen 220 files. Screenshot: docs/screenshots/auto/gallery-after-sunset.png. Backup: `Image/archie-sunset-backup-2026-05-27/`. Docs still mention bidar/av (out of scope).
  - **All viewer dev servers stopped/cleared (ports free).** To view: `pnpm -F @archie/viewer dev` (predev gen → serves complete tree on :4321). The recurring `:4321` 404 was ALWAYS a stale dev server indexing publicDir before `gen` regenerated it — never a code bug.
  - **DECISION SUPERSEDED (2026-05-27): build THREE voynich exhibits, one per layout, different stories** (user directive — see memory `voynich-three-layout-exhibits`). single=`voynich-rosettes` "The Rosettes"; grid=`voynich` "The Whole Manuscript"; narrative=`voynich-reading` "Reading the Unreadable". All over the shared `voynich.ts` seed; descriptions must NAME the layout (curator voice). Build+verify agent DISPATCHED.
  - WIRING: notes target `canvasIdFor(slug,objId)` → build a separate log per slug. Grid drops sections (so resolveLayout=grid + carousel). Each cover = IIIF derivative. Studio: 3 DEFAULT_EXHIBITS + seededFor slugs. New pages: voynich-rosettes.astro, voynich-reading.astro.
  - **FOLLOW-UP WAVE (after the 3-exhibit build lands) — 3 additions to the shared seed + sample-data:**
    1. **Base descriptive notes per folio** (reading-less, always-visible "what you see" captions, curator voice from `03`/`01`) — Wave 1 emptied `voynichNotes`=[], so the default/base view shows NO markers until a reading is toggled. Restore base captions so it's never empty. (Matches the original "captions become the always-visible BASE, Q16" intent.)
    2. **Rights/details**: per-object Beinecke MS 408 attribution (each folio), AV o12 = CC BY-NC-SA 3.0 (Kryptogramm/Schwerdtfeger), exhibit-level credit/requiredStatement on all three.
    3. **Cross-link repoint**: bidar → INTER-voynich (e.g. grid Rosettes note → `voynich-rosettes`), because bidar/av are being SUNSET (user plan) so only the 3 voynich remain as templates. Leave bidar/av in place for now (don't delete unprompted); just repoint off them.
    - Re-verify by running MUST include: default/base view shows descriptive markers (not empty).
  - **NarrativeReader readings fix — VERIFIED WORKING** (screenshot: legend Base/Cipher/Hoax/Natural-language; markers toggle per reading, pixel-diff proven; 6-section spine intact; 389/389 tests). Feeds the narrative version.
  - METHOD NOTE: Annotorious markers are WebGL-drawn → DOM count & getImageData are FALSE-NEGATIVE; verify markers via real screenshot only.
  - Leftover dev server to clean up: astro dev PID 3241072 on :4321.
  - **FIX APPLIED (option a) — superseded by three-exhibit build above.** `NarrativeReader.svelte` now takes `readings`/`activeReading`/`onreading`/`readingAnnotationsByObject`/`styleFor`, mounts `<ReadingLegend>`, merges base+active-reading notes into `activeNotes` (mirrors `ExhibitView.annotationsOf`), passes `styleOf` to Canvas. `ExhibitView.svelte` narrative branch wires its existing `activeReading` state + `readingStyleOf`. Decision per advisor: user already locked "all features in one exhibit" → extending narrative is completion, not scope creep; no user round-trip needed. Verify agent (Playwright) running: pass = legend shows 3 readings + reading-notes appear + toggle swaps them on the live voynich page.
- **`[SNAG]` fflate hydration:** cold `astro dev` can render the viewer BLANK (`optimizeDeps.include:["fflate"]` 504, `[astro-island] Error hydrating ExhibitView`). Workaround: `rm -rf apps/viewer/node_modules/.vite && astro dev --force`. Worth hardening.
- **TOOLCHAIN `[SNAG]`:** corepack pnpm shim broken under Node 24 (`ERR_VM_DYNAMIC_IMPORT_CALLBACK_MISSING`) — use `~/.npm-global/bin/pnpm` (v11) or pin `packageManager`.
- **NEEDS HUMAN (after fix):** visual-tune the APPROX xywh on live folios; listen-confirm the Kryptogramm track for AV `t=` ranges; re-host audio on one.compost.digital (CC BY-NC-SA). Then Phase 5 verify (happy-dom, NOT lightpanda).

## Build-time must-not-forget (from Wave 2)
- `exhibits.json` Voynich description is STALE ("Five folios…") → update to ~"Eleven folios… three rival interpretations".
- Precise `xywh` region coords deferred to build (Phase 4).
- AV `t=` boundaries provisional — human must listen-confirm the Kryptogramm track before build.

## Wave 1 findings → decisions pending (resolve before build)
- **IIIF-direct opportunity:** Beinecke MS 408 manifest resolves live at `collections.library.yale.edu/manifests/2002046` (IIIF Image API level2 / deep-zoom). Archie is IIIF-native → Phase 4 may point Canvases **directly at Yale's image services, no download**. Confirm this is the Phase-4 approach.
- **AV asset — RESOLVED (2026-05-27):** human approved NC. Locked = Kryptogramm track `04-f18v` (Internet Archive), **CC BY-NC-SA 3.0**. Re-host on `one.compost.digital` with attribution per SA. Still needs an ear-check before build.
- **IIIF-direct — RESOLVED (default, no objection):** Phase 4 points Canvases directly at Yale image services, no download.
- Foldout naming: use Yale's actual canvas labels/imageIds (e.g. `"85v and 86r (foldout)"`), NOT sub-indexed `f67r2` convention.

## Next steps (fresh session)
Wave-1 decisions all resolved (AV asset + IIIF-direct locked above). Cleared for:
1. Run **Phase 2 (Analysis)** = Wave 2: interpretive frameworks (cipher/hoax/natural-language) over the same regions → readings; cross-cutting motifs → tags; narrative through-line → sections. Grounds in `01-`/`02-` docs. Output: `03-analysis.md`.
2. Then Phase 3 (Ideation → design doc), Phase 4 (Build), Phase 5 (Verify).

## Watch-outs
- `packages/render-core/src/publish/voynich-readings.test.ts` pins readings behavior — update in lockstep when readings expand to all folios.
- Browser-verify with **happy-dom mount tests, NOT lightpanda** (it wedges in this workspace).
- Cross-link target `bidar` is real + published (`apps/viewer/public/published/bidar/`) — reuse, don't rebuild.
- Workspace Node: use **fnm-managed Node v24** for pnpm/corepack (system v20 is incompatible).
