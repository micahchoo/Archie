---
// Deliberate CI tripwire test (ISSUES.md Issue 1) — proves the astro-check job catches a real
// diagnostic that bare `tsc --noEmit` never sees (it doesn't parse .astro files). Reverted next commit.
---
<p>{undefinedTripwireVariable}</p>
