{
  "name": "@render/svelte",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin Svelte adapter over @render/mount. Owns reactivity (binds selectedId to setSelected, subscribes onSelect). Dependency-hygiene seam: the viewer does not depend on @render/mount directly (fixture-reach.test.ts), so it reaches mount's named pieces through this barrel. Budget <500 LOC = logic-leak detector (ADR-0002 / Q-2).",
  "license": "GPL-3.0-only",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./Canvas.svelte": "./src/Canvas.svelte",
    "./ResizeDivider.svelte": "./src/ResizeDivider.svelte"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "node ../../node_modules/typescript-native/bin/tsc --noEmit"
  },
  "dependencies": {
    "@render/core": "workspace:*",
    "@render/mount": "workspace:*"
  },
  "peerDependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "svelte": "^5.15.0"
  }
}
