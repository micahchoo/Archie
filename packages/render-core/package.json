{
  "name": "@render/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure-TS rendering core: WADM types, annotation spine (append-only log -> version DAG -> heads/history), selector/URL/IIIF/geometry, Filesystem seam. No framework, no DOM-runtime deps. (ADR-0002 / Q-2)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./spine": "./src/spine/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
