{
  "name": "@render/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pure-TS rendering core: WADM types, annotation spine (append-only log -> version DAG -> heads/history), selector/URL/IIIF/geometry, Filesystem seam. No framework, no DOM-runtime deps. The exports map IS the public API — deep imports are unsupported. (ADR-0002 / Q-2)",
  "license": "GPL-3.0-only",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": [
    "./src/text/sanitize.ts"
  ],
  "exports": {
    ".": "./src/index.ts",
    "./spine": "./src/spine/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "node ../../node_modules/typescript-native/bin/tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "isomorphic-dompurify": "^3.18.0",
    "snarkdown": "^2.0.0"
  }
}
