{
  "name": "@render/mount",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Framework-free vanilla mount functions wiring OpenSeadragon + Annotorious headless + Wavesurfer. Exposes an imperative surface (fitBounds/setSelected/destroy) + onSelect callback the adapter owns. Filled in Phase 1 (spike-0001 module 1 delamination). (ADR-0002 / Q-2)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@render/core": "workspace:*",
    "openseadragon": "^5.0.1",
    "@annotorious/openseadragon": "^3.8.2",
    "@annotorious/plugin-tools": "^1.6.0"
  },
  "devDependencies": {
    "@types/openseadragon": "^5.0.2",
    "happy-dom": "^20.9.0"
  }
}
