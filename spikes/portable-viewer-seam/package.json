{
  "name": "spike-portable-viewer-seam",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QUARANTINED throwaway spike — read-seam comparison for the portable Viewer (feeds ADR-0010). NOT in pnpm-workspace globs; do not wire into the app build.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "happy-dom": "^15.0.0",
    "fflate": "^0.8.2",
    "typescript": "^5.6.3"
  }
}
