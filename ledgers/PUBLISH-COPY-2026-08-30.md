# PUBLISH-COPY ledger — Archie-495d

Claim-vs-reality diff over the publish surface's done-screens (`done-download` [zip],
`zip-options`, `done-folder`, `done-object`, `done-deposit`, the GitHub wizard success screen) and
the `ExportMenu` entry copy, in `apps/studio/src/Publish.svelte` + `apps/studio/src/ExportMenu.svelte`.
Every factual claim was checked against the writer code and its tests (read-only evidence):
`apps/studio/src/publish-self-contained.test.ts`, `apps/studio/src/publish-flows.svelte.ts`,
`apps/studio/src/export-surface.ts`, `apps/studio/src/single-file-export.ts` (+ test),
`apps/studio/src/share-link.ts` (+ test), `apps/studio/src/collab-attribution.ts` (+ test),
`packages/render-core/src/publish/static-pages.ts`, `packages/render-core/src/publish/site.ts` (+ test),
`packages/render-core/src/publish/fixity.ts`, `packages/render-core/src/publish/ghpages.ts`,
`packages/render-core/src/publish/marker.ts` (+ test), `packages/render-core/src/publish/portable.ts` (+ test),
`packages/archie-viewer/build.mjs`, `packages/archie-viewer/dist-single/archie-viewer.single.js`,
`apps/viewer/astro.config.mjs`, `apps/viewer/package.json`.

Type vocabulary: `claimed-not-implemented` / `implemented-differently` / `implemented-not-documented`
(defects), `accurate` (verified against the writer, no change).

Commit column: the copy fixes landed in `5dcdf34`; the completed ledger (this content) lands in a
small follow-up commit, after a concurrent-slice commit race orphaned the amend that would have
carried it. The defect rows cite both.

| claim | where claimed | what the artifact actually does | type | resolution | commit | recheck |
|---|---|---|---|---|---|---|
| Folder screen: "To look at it first, serve the folder locally: `pnpm --filter @archie/viewer dev`" then "Then open `http://localhost:4321`." | Publish.svelte done-folder (~:753-755, pre-fix) | The folder destination ships its own self-contained reader — `viewer.html` + `_viewer/archie-viewer.js` + `.nojekyll` (publish-self-contained.test.ts:88-110; static-pages.ts `viewerShellHtml`, tree-relative). ANY static file server serves it; none was named. The pnpm command starts THIS repo's viewer dev server (`apps/viewer` predev `gen-published` → `astro dev`, astro.config.mjs) — the repo's front-door/fixture site, a different origin and a different site than the one just written. | claimed-not-implemented | Copy replaced: "serve the folder with any static file server — it carries its own reader", example `python3 -m http.server 8000` → `http://localhost:8000`. | 5dcdf34 (copy fixes) + ledger follow-up | re-serve a written folder with the example command; viewer loads over the folder's own tree |
| Folder screen: "Re-publish here any time — Archie replaces what's there and clears out what you deleted." | Publish.svelte done-folder (~:752, pre-fix) | A folder re-publish is a FULL write that overwrites changed files but prunes NOTHING: publish never removes a file except through `removedExhibits`/`removedObjects` (fixity.ts:89-93; site.test.ts:724-732 "a FULL publish … never overwrites q away"), and the folder destination passes no removals (publish-flows.svelte.ts:939-945 `writeTree(fb.fs, { withViewer: true })`). Deleted exhibits' directories persist. (GitHub Pages DOES clear them — push-delta replaces the whole tree.) | claimed-not-implemented | Copy now says re-publish "rewrites the site in place" and states the caveat: a deleted exhibit stays in the folder until removed by hand; GitHub Pages clears deleted ones. | 5dcdf34 (copy fixes) + ledger follow-up | publish folder, delete an exhibit, re-publish same folder, observe orphan dir |
| Zip link screen: "To publish something that stands on its own, use 'To GitHub Pages' instead." | Publish.svelte done-download (~:740, pre-fix) | Understated since Archie-e09d: GitHub Pages AND the folder destination both produce a tree that stands on its own — the folder ships the same reader bundle (publish-self-contained.test.ts:88-117); the single-file refusal copy already steers there (Publish.svelte exportSelfContained error). | implemented-not-documented | Copy now: "publish the site itself — 'To GitHub Pages', or the folder with the reader built in." | 5dcdf34 (copy fixes) + ledger follow-up | read done-download against both self-standing routes |
| Zip screen: "someone else can open it, add their own notes, and send it back to you" / "they open it in their own Archie, annotate their pass, and send it back. Open their copy here and Archie shows who added what." | Publish.svelte done-download :700, :719 | Accurate: the zip carries the annotation logs with `lastEditor` stamps (identity audit; publish ships what READS, publish-warnings.ts); on open, `collab-attribution.ts` (Archie-90f1) renders "Meera · 2d ago" chips + a filter-by-editor lens, gated on ≥2 distinct editors; conflicts surface via MergeReview (App.svelte). Send-back = the same `saveProjectZip`. | accurate | none | — | — |
| Zip screen: "Keep it yourself too, as a backup or to re-open here any time." / "upload the zip anywhere public — your site, a GitHub release, the Internet Archive — then paste its URL" | Publish.svelte done-download :720-722 | Accurate: a `.archie.zip` reopens locally (`openArchieLibrary`, ADR-0020 marker gates non-Archie zips); a public URL is fetched by the share path (`openArchieLibraryFromUrl` / `fetchZipBytesIfAny`, `SRC_MAX_BYTES` cap — open.ts). | accurate | none | — | — |
| Zip screen: "Or embed the exhibit in a blog, LMS page, or site. Two ways:" + WC ("recommended… one-time script tag") vs iframe ("fallback for hosts that strip scripts") | Publish.svelte done-download :725-729 | Accurate: two minted snippets — `wcSnippet` (CDN_RUNTIME module script + `<archie-viewer src=zip>`, ADR-0021 contract) and `viewerEmbedSnippet(shareLink)` iframe (ADR-0006 floor); share-link.test.ts:60-71 proves the iframe embeds the SAME round-tripping link. | accurate | none | — | — |
| Zip screen broken-assets advisory: "{n} image(s) aren't in the saved file… re-add the originals, then save again." | Publish.svelte done-download :704-717 | Accurate: `missingAssets` from the projection (`saveProjectZip` → `s.missingAssets`, publish-flows :851-860) — the round-trip-loss advisory, shown on the flow that produced the zip. | accurate | none | — | — |
| Zip options screen: "Name the file and choose what goes in it." + "Notes that link to an exhibit you've left out will show as plain text in the copy." | Publish.svelte zip-options :678-694 | Accurate: `libraryForZip(slugs)` filters after `buildFullLibrary`; an omitted exhibit's cite degrades to plain text via publishLibrary's link index and surfaces in brokenLinks (publish-flows :591-606). | accurate | none | — | — |
| Zip link screen: link depends on uploaded zip + viewer at {CANONICAL_HOST}; `#/?src=` grammar | Publish.svelte done-download :740, :327 | Accurate: `viewerShareLink` mints `#/?src=` inside the hash (share-link.test.ts round-trips against the viewer's `parseRoute`); canonical host from archie.config.json. | accurate | none | — | — |
| Zip screen: "Downloaded your `.archie.zip`." / Web Component + iframe snippets | Publish.svelte done-download :703, :726-729 | Accurate: `done-download` is only reachable via `saveProjectZip().saved` (publish-flows :623-644; Publish.svelte:506-515); wcSnippet uses CDN_RUNTIME script + `<archie-viewer src=zip>`; `viewerEmbedSnippet` embeds the same share link (share-link.test.ts:60-71). | accurate | none | — | — |
| Folder screen lede: "A finished website — every page, image and note, no server needed to build it." | Publish.svelte done-folder :748 | Accurate: the projection emits static pages (library + per-exhibit, full note texts with anchors), assets/thumbnails/tiles, and structure history; building is a pure local write — no server involved. | accurate | none | — | — |
| Folder screen: "Upload the whole folder to any web host and it works as it is." | Publish.svelte done-folder :752 | Accurate (kept): self-contained reader + relative-first tree + `.nojekyll` for Jekyll hosts. | accurate | none | — | — |
| ExportMenu zip row: "The whole library plus your original files, in one file you can keep, send, or open again in Archie." | ExportMenu.svelte :77-78 | Accurate: zip = published tree + `archie.json` self-ID marker (marker.test.ts, ADR-0020); imported assets ship via `getAsset` (archival pass-through = original bytes; portable.test.ts round-trips assets back out); reopens via `openArchieLibrary`. Round-trip-loss advisory covers the missing-original case. | accurate | none | — | — |
| ExportMenu single-file row: "Opens by double-click — no server, no account, no internet. Search isn't in it. Past about {50} MB Archie will point you at the folder below instead." | ExportMenu.svelte :91-92 | Accurate: `buildSingleFileHtml` inlines an IIFE bundle + base64 library; tests forbid module scripts/relative fetches (single-file-export.test.ts); the single bundle contains no search (no minisearch in `packages/archie-viewer/dist-single/archie-viewer.single.js`); cap is `SINGLE_FILE_MAX_BYTES` = 50 MB, refusal message names the folder route (publish-flows :823-842, Publish.svelte :485). | accurate | none | — | — |
| ExportMenu folder row: "The finished site … carrying its own reader. No size limit. Put it on any web host or hand over the whole folder." / unavailable reason "needs the desktop app or Chrome" | ExportMenu.svelte :98-102 | Accurate: folder destination writes `viewer.html` + `_viewer/` + `.nojekyll` (publish-self-contained.test.ts:88-110); tree is relative-first so any host/subpath serves it (static-pages.ts:222-250); no byte ceiling on the folder sink (ceiling guards exist only for eager zip paths, publish-flows :104-131); `folderSinkSupported()` gates the row (folder-backend, export-surface rule). | accurate | none | — | — |
| ExportMenu deposit row: "Every published file with a checksum beside it, in the BagIt layout repositories ask for." | ExportMenu.svelte :110-111 | Accurate: `writeBag` (RFC 8493) — payload under `data/`, SHA-256 per file (fixity.ts), `bag-info.txt`; deposit screen names `data/`, `manifest-sha256.txt`, `bag-info.txt` correctly. | accurate | none | — | — |
| ExportMenu header: "None of these changes where your library is published." | ExportMenu.svelte :70 | Accurate: rows route to existing artifact sinks only; the site half lives in SetupFlow/PublishSheet (ExportMenu.svelte:2-12). | accurate | none | — | — |
| Object-storage screen: two rclone commands; second line "sends the one small file that tells a reader the library is complete, and it has to arrive after everything else"; CORS note; "rclone works out what actually moved" | Publish.svelte done-object :780-795 | Accurate: `rcloneCommands` = `rclone sync … --exclude archie.json` then `rclone copyto …/archie.json` (export-surface.ts:236-240; sequencing guarantees the marker lands last — the measured concurrent-sync defect is documented at Publish.svelte:781-784); `archie.json` is the tree root self-ID/completeness marker (marker.ts, ADR-0020); BUCKET_CORS_NOTE matches export-surface.ts:244-245; sync is delta-transfer. | accurate | none | — | — |
| Deposit screen: "The whole library, with a checksum recorded for every file in it." / "{n} files, each with its own SHA-256." / data/ + manifest-sha256.txt + bag-info.txt | Publish.svelte done-deposit :799-810 | Accurate: `writeBag` hashes every payload file (fixity re-reads its own manifest + `archie.json` to hash them too — bag.ts header); `payloadFiles` is the writer's real count, rendered via `toLocaleString()`. | accurate | none | — | — |
| GitHub success screen: "Your site is live." / first-build delay / "Just hit Publish again — it updates the same site." | Publish.svelte wizard `success` :997-1009 | Accurate: `pagesUrlFor` computes the visitor URL; push-delta republishes incrementally into the same site (ghpages.ts git-trees "replace this tree"; planPush); Pages first build lag is real (also manual-pages copy). | accurate | none | — | — |

## Resolution summary

Copy fixes landed in `apps/studio/src/Publish.svelte` (done-download :740; done-folder :752-755),
commit `5dcdf34`. Two claimed-not-implemented defects (serve instructions; prune claim) and one
implemented-not-documented gap (the folder route also stands on its own) fixed; everything else —
including the hand-off/attribution claims and the zip-options subset warning, verified on a second
contiguous pass — is accurate and left untouched. Final wording goes to the user for review after
landing (per dispatcher note). Writer code/tests untouched (read-only evidence).
