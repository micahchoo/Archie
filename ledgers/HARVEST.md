# HARVEST — cross-session lessons from tend runs

One dated section per session. Format: lesson | rule | stored where.

## 2026-07-05 — Issue 7 (ledgers/NEGSPACE.md)

| lesson | rule | stored where |
|---|---|---|
| A prior session's Issues 5 (CANON.md) and 6 (COVERAGE.md) had finished, verified work sitting uncommitted after a `/clear` — discovered only because Issue 7's fix touched the same files. | Before starting a new tend loop, run `git status`/`git diff --stat` first; if prior-issue work is sitting uncommitted, resolve it (commit or reconcile) before layering new edits on the same files. | `.claude/CLAUDE.md` candidate — not added this session (repo-specific workflow habit, not a code rule; flagged here for the user to decide whether it belongs in CLAUDE.md or stays a personal habit) |
| Issue 7's own inventory almost re-flagged `openZip`'s `"invalid zip data"` message as a new bug — Issue 4's `SILENCE.md` row 32 had already examined that exact site and deliberately chosen to surface it verbatim. | Before logging a finding as new, grep sibling `ledgers/*.md` for the same file:line — a prior loop may have already decided it. | Already the tend skill's own stated discipline ("recorded decisions are not re-litigated without new evidence"); this session is the concrete case that proves why it matters. No new storage needed. |
| `apps/studio` has no Svelte component-mount test harness (no `@testing-library/svelte`, zero existing component tests) — surfaced when the transcript-cap fix (AvEditor.svelte) needed one and didn't get one. | If a future fix needs to assert Svelte component *behavior* (not just a pure/exported function), building the harness is a separate, larger decision — don't build it silently inside an unrelated fix's commit. | Noted here; not acted on (disproportionate to Issue 7's "Worth exploring" strength) |
| Multi-item async ingest loops (`newExhibitFromManifest`, `newExhibitFromFolder`, `addFiles`) all re-read `ctx.currentSlug()` live on every iteration with no blocking overlay during import — the general shape of the bug, not just these three instances. | A new multi-item import loop added to `ingest-flows.ts` must pin its target exhibit slug once at the top and thread it through `appendObject`/`addObjectFromFile`, never read `ctx.currentSlug()` mid-loop. | Recorded in `ledgers/NEGSPACE.md` rows 3/4 and as inline comments at each of the three fixed call sites — the rule lives where the next loop's author will actually read it. |

## 2026-07-05 — Issue 2 (ledgers/CLAIMS.md)

| lesson | rule | stored where |
|---|---|---|
| Almost resolved `HANDOFF.md` by tombstoning it ("superseded, read elsewhere") before checking that `CLAUDE.md` instructs actively maintaining it for session continuity — a tombstone would have broken that convention, not fixed the doc. | Before "deprecating" a doc a tend loop finds stale, check whether it's also referenced by an operating instruction (CLAUDE.md, a skill) that expects it to be a *living* file — if so, rewrite to current state instead of archiving. | Applied directly in `HANDOFF.md`'s own rewrite; noted here as the general case. |
| Dispatching 3 parallel research agents for a claim-diff worked well for coverage, but none of them proactively pushed their final report to the main thread — each went idle after finishing, requiring an explicit "send your report now" ping before the result was visible. | When spawning parallel research agents whose output must be compiled into a ledger, instruct them explicitly to send the final structured report as their literal last action (not just return text), and expect to ping idle agents rather than assume idle = reported. | Noted here; would help future multi-agent claim-diffs run without the extra round-trip. |
| A first-pass agent flagged README's "Map annotation" feature row as unconfirmed (it searched for Studio-only module names like `AddMapModal.svelte` inside `apps/viewer/src` and found nothing) — a quick manual check found the Viewer correctly consumes the shared `tileSource` render path instead, so the feature was accurate all along. | An agent's "couldn't confirm" on an architecturally-split feature (authoring-only module absent from the read-only app) is not evidence of a gap — check the shared/consuming layer before accepting a not-found as a real finding. | Applied directly in `ledgers/CLAIMS.md`'s Map-annotation row; noted here as the general case. |

## 2026-07-05 — Issue 8 (ledgers/COLDSTART.md)

| lesson | rule | stored where |
|---|---|---|
| The original ISSUES.md diagnosis framed this as "fresh clone loses LFS" — the rehearsal found the opposite starting point: a plain fresh clone gets *working* LFS hooks (git-lfs self-installs them into `.git/hooks` during checkout). It's running the repo's own `qa/hooks/install.sh` that regresses a working setup to a broken one, by redirecting `core.hooksPath` to a directory missing the untracked hook files. | When a rehearsal's actual observed sequence contradicts the seed diagnosis's framing, trust the rehearsal — re-characterize the bug precisely (here: "the setup step breaks it," not "the clone lacks it") rather than fixing to match the original wording. | Recorded directly in `ledgers/COLDSTART.md`'s "The real bug, precisely characterized" section. |
| A cold-start rehearsal on a sandboxed dev environment can't exercise "install Node/pnpm/git-lfs from scratch" because those tools are already present system-wide — the rehearsal can only honestly cover the repo-specific setup surface. | State the scoping caveat explicitly in the ledger up front, rather than letting a clean rehearsal silently imply more was verified than actually was. | Recorded as this ledger's opening "Scoping caveat" line — the pattern to reuse in any future cold-start-style loop run in this same sandbox. |

## 2026-07-05 — Issue 9 (ledgers/SHOWROOM.md)

| lesson | rule | stored where |
|---|---|---|
| Spent a full research/planning turn asking the user to choose between "hand-built" vs "seed fixture" for the showroom before checking whether the work already existed — the user's one-line correction ("this is the showroom") immediately outdated the whole question. A committed `apps/viewer/libraries/archie-library.archie.zip` was sitting there, ~95% assembled, the whole time. | Before asking the user to decide *how* to build something, check whether it's already been built (`git log`, existing artifacts named in the issue's own symptom text, sibling drop-folders) — a discovery question ("does this exist?") is cheaper than a decision question and can make the decision question moot. | Recorded here; no single file to anchor it to since it's a search-before-ask habit, not a code rule. |
| Chased a "wrong exhibit's content is rendering" phantom bug for a long stretch (multiple Playwright sessions, cache-clearing, reading component internals) before realizing the screenshot in question was *supposed* to show a different exhibit's content — it was a picture of the narrative reader, and the app's only narrative exhibit is Voynich, so a screenshot of "the narrative reader in action" naturally depicts Voynich. | Before debugging a "shows the wrong data" symptom as a data/caching bug, check what the artifact (image, fixture, example) is *supposed to contain* — a correctly-rendered picture of something else is not the same failure mode as a data leak, and looks identical at a glance. | Recorded in `ledgers/SHOWROOM.md`'s "One false alarm" section. |
| Two real, previously-unexposed bugs (`loadLibrary` dropping sections/readings; `gen-published.mts` never wiring `getAsset`) had survived undetected because every existing bundled exhibit either has no narrative/readings, or only ever references external-URL media — the showroom was the first content to exercise either path in the "drop a zip into the publish folder" flow. | A feature path with zero real exercise (every existing example avoids it) is unverified regardless of how long it's been in the codebase — treat "nothing has ever used this branch" as a specific reason to test it, not evidence it's fine. | Fixed directly (`6c61cf2`, `a025485`), each with its own regression test; recorded here as the general pattern. |
| A one-off data-assembly script (`assemble.mts`) hardcoded its own output path — re-running it after fixing an upstream bug silently overwrote the last-known-good file with a new test variant, and a later step copied the wrong version into the real repo without noticing (env vars from an `export` in one Bash call don't persist to the next, which masked the actual cause for a while). | For a multi-run throwaway script, either parameterize the output path per run or immediately copy+rename the output to a distinctly-named file before the next run — never trust "the script's default output path" to still hold the version you think it does after any re-run. | Recorded here; applied ad hoc mid-session (`finished-PRODUCTION.archie.zip`) once noticed. |

## 2026-07-05 — Issue 10 (ledgers/METHOD.md)

| lesson | rule | stored where |
|---|---|---|
| A methodology document can drift the same way a feature-status document does — `IMPLEMENTATION-STRATEGY.md` named 13 tools/skills (`sd`, `mulch`, `gate-enforcer`, `qmd`, ...) that had all quietly stopped existing, discovered only as a side effect of Issue 2's unrelated claim-diff. | When auditing a repo's docs for drift, don't stop at feature-status claims — a process/methodology doc that tells the next session (human or AI) *how to work here* can be just as stale, and is read just as early. | Already covered by the tend skill's own scope (Issue 2's "operational docs" framing); this session's concrete find is the evidence it applies to methodology docs specifically, not just status docs. |

## 2026-07-05 — Directions 1, 2, 3 (ledgers/CAPABILITY.md, ledgers/DARKDATA.md)

| lesson | rule | stored where |
|---|---|---|
| The user gave a blanket "pursue" verdict for all three directions up front, before the inventory phase ran. This did not shortcut the loop — the direction pattern's own discipline (inventory everything, THEN verdict, THEN commission) still had to run in full for the verdict to mean anything: a "pursue" without knowing what's being pursued is not a real decision. | A pre-given verdict changes who answers the verdict question, not whether the inventory happens — still enumerate every row with evidence before writing "pursue," so the commissioned next step (spec interview / prototype brief) is scoped to something real. | Applied directly across all three ledgers' verdict sections; recorded here as the general case for future direction loops run with an advance verdict. |
| Direction 2's own framing (written by an earlier tend pass) assumed `lastEditor`'s only display consumer was the unmounted `MergeReview.svelte`. The fresh inventory found a second, currently-live consumer (`collab.ts`'s aggregate per-editor banner) that the original framing missed. | A direction's own "Surplus" write-up in ISSUES.md is a hypothesis from an earlier, shallower pass — re-verify it during the loop's inventory phase rather than treating it as already-confirmed evidence; the loop is designed to deepen the finding, not just execute it. | Corrected in `ledgers/DARKDATA.md`'s cluster summary for identity values; recorded here as the general pattern. |
| Direction 3's ISSUES.md write-up hedged the dialog gap as "`unknown` — likely just sequencing." A git-history check (same commit shipped both the src-only dialog and the element's full `observedAttributes` list) found evidence against pure sequencing, leaning the intent toward deliberate v1 scoping instead. | When a direction's "intent" field is marked `unknown`, a cheap git-log/git-blame check on the two sides of the gap (when did the capability land vs. when did the consuming UI last change) can often convert `unknown` into an evidenced call — do this before leaving intent as a shrug. | Applied directly in `ledgers/CAPABILITY.md`'s Direction 3 section; recorded here as the general pattern. |
