# HARVEST — cross-session lessons from tend runs

One dated section per session. Format: lesson | rule | stored where.

## 2026-07-05 — Issue 7 (ledgers/NEGSPACE.md)

| lesson | rule | stored where |
|---|---|---|
| A prior session's Issues 5 (CANON.md) and 6 (COVERAGE.md) had finished, verified work sitting uncommitted after a `/clear` — discovered only because Issue 7's fix touched the same files. | Before starting a new tend loop, run `git status`/`git diff --stat` first; if prior-issue work is sitting uncommitted, resolve it (commit or reconcile) before layering new edits on the same files. | `.claude/CLAUDE.md` candidate — not added this session (repo-specific workflow habit, not a code rule; flagged here for the user to decide whether it belongs in CLAUDE.md or stays a personal habit) |
| Issue 7's own inventory almost re-flagged `openZip`'s `"invalid zip data"` message as a new bug — Issue 4's `SILENCE.md` row 32 had already examined that exact site and deliberately chosen to surface it verbatim. | Before logging a finding as new, grep sibling `ledgers/*.md` for the same file:line — a prior loop may have already decided it. | Already the tend skill's own stated discipline ("recorded decisions are not re-litigated without new evidence"); this session is the concrete case that proves why it matters. No new storage needed. |
| `apps/studio` has no Svelte component-mount test harness (no `@testing-library/svelte`, zero existing component tests) — surfaced when the transcript-cap fix (AvEditor.svelte) needed one and didn't get one. | If a future fix needs to assert Svelte component *behavior* (not just a pure/exported function), building the harness is a separate, larger decision — don't build it silently inside an unrelated fix's commit. | Noted here; not acted on (disproportionate to Issue 7's "Worth exploring" strength) |
| Multi-item async ingest loops (`newExhibitFromManifest`, `newExhibitFromFolder`, `addFiles`) all re-read `ctx.currentSlug()` live on every iteration with no blocking overlay during import — the general shape of the bug, not just these three instances. | A new multi-item import loop added to `ingest-flows.ts` must pin its target exhibit slug once at the top and thread it through `appendObject`/`addObjectFromFile`, never read `ctx.currentSlug()` mid-loop. | Recorded in `ledgers/NEGSPACE.md` rows 3/4 and as inline comments at each of the three fixed call sites — the rule lives where the next loop's author will actually read it. |

## 2026-07-05 — Issue 2 (ledgers/CLAIMS.md)

| lesson | rule | stored where |
|---|---|---|
| Almost resolved `HANDOFF.md` by tombstoning it ("superseded, read elsewhere") before checking that `CLAUDE.md` instructs actively maintaining it for session continuity — a tombstone would have broken that convention, not fixed the doc. | Before "deprecating" a doc a tend loop finds stale, check whether it's also referenced by an operating instruction (CLAUDE.md, a skill) that expects it to be a *living* file — if so, rewrite to current state instead of archiving. | Applied directly in `HANDOFF.md`'s own rewrite; noted here as the general case. |
| Dispatching 3 parallel research agents for a claim-diff worked well for coverage, but none of them proactively pushed their final report to the main thread — each went idle after finishing, requiring an explicit "send your report now" ping before the result was visible. | When spawning parallel research agents whose output must be compiled into a ledger, instruct them explicitly to send the final structured report as their literal last action (not just return text), and expect to ping idle agents rather than assume idle = reported. | Noted here; would help future multi-agent claim-diffs run without the extra round-trip. |
| A first-pass agent flagged README's "Map annotation" feature row as unconfirmed (it searched for Studio-only module names like `AddMapModal.svelte` inside `apps/viewer/src` and found nothing) — a quick manual check found the Viewer correctly consumes the shared `tileSource` render path instead, so the feature was accurate all along. | An agent's "couldn't confirm" on an architecturally-split feature (authoring-only module absent from the read-only app) is not evidence of a gap — check the shared/consuming layer before accepting a not-found as a real finding. | Applied directly in `ledgers/CLAIMS.md`'s Map-annotation row; noted here as the general case. |

## 2026-07-05 — Issue 8 (ledgers/COLDSTART.md)

| lesson | rule | stored where |
|---|---|---|
| The original ISSUES.md diagnosis framed this as "fresh clone loses LFS" — the rehearsal found the opposite starting point: a plain fresh clone gets *working* LFS hooks (git-lfs self-installs them into `.git/hooks` during checkout). It's running the repo's own `qa/hooks/install.sh` that regresses a working setup to a broken one, by redirecting `core.hooksPath` to a directory missing the untracked hook files. | When a rehearsal's actual observed sequence contradicts the seed diagnosis's framing, trust the rehearsal — re-characterize the bug precisely (here: "the setup step breaks it," not "the clone lacks it") rather than fixing to match the original wording. | Recorded directly in `ledgers/COLDSTART.md`'s "The real bug, precisely characterized" section. |
| A cold-start rehearsal on a sandboxed dev environment can't exercise "install Node/pnpm/git-lfs from scratch" because those tools are already present system-wide — the rehearsal can only honestly cover the repo-specific setup surface. | State the scoping caveat explicitly in the ledger up front, rather than letting a clean rehearsal silently imply more was verified than actually was. | Recorded as this ledger's opening "Scoping caveat" line — the pattern to reuse in any future cold-start-style loop run in this same sandbox. |

## 2026-07-05 — Issue 9 (ledgers/SHOWROOM.md)

| lesson | rule | stored where |
|---|---|---|
| Spent a full research/planning turn asking the user to choose between "hand-built" vs "seed fixture" for the showroom before checking whether the work already existed — the user's one-line correction ("this is the showroom") immediately outdated the whole question. A committed `apps/viewer/libraries/archie-library.archie.zip` was sitting there, ~95% assembled, the whole time. | Before asking the user to decide *how* to build something, check whether it's already been built (`git log`, existing artifacts named in the issue's own symptom text, sibling drop-folders) — a discovery question ("does this exist?") is cheaper than a decision question and can make the decision question moot. | Recorded here; no single file to anchor it to since it's a search-before-ask habit, not a code rule. |
| Chased a "wrong exhibit's content is rendering" phantom bug for a long stretch (multiple Playwright sessions, cache-clearing, reading component internals) before realizing the screenshot in question was *supposed* to show a different exhibit's content — it was a picture of the narrative reader, and the app's only narrative exhibit is Voynich, so a screenshot of "the narrative reader in action" naturally depicts Voynich. | Before debugging a "shows the wrong data" symptom as a data/caching bug, check what the artifact (image, fixture, example) is *supposed to contain* — a correctly-rendered picture of something else is not the same failure mode as a data leak, and looks identical at a glance. | Recorded in `ledgers/SHOWROOM.md`'s "One false alarm" section. |
| Two real, previously-unexposed bugs (`loadLibrary` dropping sections/readings; `gen-published.mts` never wiring `getAsset`) had survived undetected because every existing bundled exhibit either has no narrative/readings, or only ever references external-URL media — the showroom was the first content to exercise either path in the "drop a zip into the publish folder" flow. | A feature path with zero real exercise (every existing example avoids it) is unverified regardless of how long it's been in the codebase — treat "nothing has ever used this branch" as a specific reason to test it, not evidence it's fine. | Fixed directly (`6c61cf2`, `a025485`), each with its own regression test; recorded here as the general pattern. |
| A one-off data-assembly script (`assemble.mts`) hardcoded its own output path — re-running it after fixing an upstream bug silently overwrote the last-known-good file with a new test variant, and a later step copied the wrong version into the real repo without noticing (env vars from an `export` in one Bash call don't persist to the next, which masked the actual cause for a while). | For a multi-run throwaway script, either parameterize the output path per run or immediately copy+rename the output to a distinctly-named file before the next run — never trust "the script's default output path" to still hold the version you think it does after any re-run. | Recorded here; applied ad hoc mid-session (`finished-PRODUCTION.archie.zip`) once noticed. |

## 2026-07-05 — Issue 10 (ledgers/METHOD.md)

| lesson | rule | stored where |
|---|---|---|
| A methodology document can drift the same way a feature-status document does — `IMPLEMENTATION-STRATEGY.md` named 13 tools/skills (`sd`, `mulch`, `gate-enforcer`, `qmd`, ...) that had all quietly stopped existing, discovered only as a side effect of Issue 2's unrelated claim-diff. | When auditing a repo's docs for drift, don't stop at feature-status claims — a process/methodology doc that tells the next session (human or AI) *how to work here* can be just as stale, and is read just as early. | Already covered by the tend skill's own scope (Issue 2's "operational docs" framing); this session's concrete find is the evidence it applies to methodology docs specifically, not just status docs. |

## 2026-07-05 — Directions 1, 2, 3 (ledgers/CAPABILITY.md, ledgers/DARKDATA.md)

| lesson | rule | stored where |
|---|---|---|
| The user gave a blanket "pursue" verdict for all three directions up front, before the inventory phase ran. This did not shortcut the loop — the direction pattern's own discipline (inventory everything, THEN verdict, THEN commission) still had to run in full for the verdict to mean anything: a "pursue" without knowing what's being pursued is not a real decision. | A pre-given verdict changes who answers the verdict question, not whether the inventory happens — still enumerate every row with evidence before writing "pursue," so the commissioned next step (spec interview / prototype brief) is scoped to something real. | Applied directly across all three ledgers' verdict sections; recorded here as the general case for future direction loops run with an advance verdict. |
| Direction 2's own framing (written by an earlier tend pass) assumed `lastEditor`'s only display consumer was the unmounted `MergeReview.svelte`. The fresh inventory found a second, currently-live consumer (`collab.ts`'s aggregate per-editor banner) that the original framing missed. | A direction's own "Surplus" write-up in ISSUES.md is a hypothesis from an earlier, shallower pass — re-verify it during the loop's inventory phase rather than treating it as already-confirmed evidence; the loop is designed to deepen the finding, not just execute it. | Corrected in `ledgers/DARKDATA.md`'s cluster summary for identity values; recorded here as the general pattern. |
| Direction 3's ISSUES.md write-up hedged the dialog gap as "`unknown` — likely just sequencing." A git-history check (same commit shipped both the src-only dialog and the element's full `observedAttributes` list) found evidence against pure sequencing, leaning the intent toward deliberate v1 scoping instead. | When a direction's "intent" field is marked `unknown`, a cheap git-log/git-blame check on the two sides of the gap (when did the capability land vs. when did the consuming UI last change) can often convert `unknown` into an evidenced call — do this before leaving intent as a shrug. | Applied directly in `ledgers/CAPABILITY.md`'s Direction 3 section; recorded here as the general pattern. |

## 2026-07-05 — graft run (DIVERGENCES.md, ledgers/PROBE-publish-to-web.md)

| lesson | rule | stored where |
|---|---|---|
| Mid-run, a concurrent session committed three Direction verdicts (`ea5fe8f`–`246550d`); the freshly-written DIVERGENCES.md already cited those Directions as "queued" and needed a mid-run correction. | In this multi-session repo, `git log --oneline <session-start-sha>..HEAD` immediately before writing any file that cites backlog statuses — a backlog line read at session start is a snapshot, not a fact. | Applied in DIVERGENCES.md's seam-constraint update; recorded here; extends the existing concurrent-session memory. |
| Creating the probe branch via `git worktree add -b probe/<name> <scratchpad-dir> main` gave the probe its own HEAD — the shared checkout never left `main`, so the known concurrent-session HEAD hazard couldn't fire at all. | For any branch-bound side work (probes, spikes) in this repo, prefer an isolated worktree over `git checkout` — it makes the shared-HEAD hazard structurally impossible instead of vigilance-dependent. | Memory file `feedback_concurrent_session_shared_git_head.md` (updated this session); recorded here. |
| All three background Explore agents went idle without delivering their reports; a one-line SendMessage nudge ("send your report to main") retrieved each in full — no respawn needed. | When a background agent signals idle with no output, ask it for the deliverable before considering the work lost or redoing it — the context is still alive in that agent. | Recorded here (harness technique, no repo file to anchor). |
| The probe's first run refuted its own mechanism (GitHub secondary rate limit kills per-file REST uploads at ~180 content-POSTs/min — 554 blobs at 20-way concurrency 403'd in 11s), and the revised mechanism (single-pack git push) then went live in 0.6 min. The kill criterion never moved; only the mechanism row was superseded. | For any GitHub bulk-upload feature: one pack push (git2/gitoxide or system git), never per-file REST — secondary rate limits make the REST path structurally wrong for tile-heavy trees. Probe refutations are results: supersede the assumption row, keep the criterion. | `ledgers/PROBE-publish-to-web.md` rows A2/A6 + its "What the build session inherits" section; recorded here for the next GitHub-integration feature. |
| In the pr-faq interview the user chose bigger than the lean recommendation on both sizing questions: appetite 3–4 weeks over the recommended ~2, and both-apps reach (browser guided-manual path) over Tauri-only. One session's data, but the direction was consistent. | When proposing defaults for scope/appetite on this project, present the lean option but expect the user to size up — include the "what the bigger version adds" detail in the option text so the upgrade is an informed pick, not a rejection of the default. | Recorded here only (single-session signal; promote to memory if the pattern repeats). |

## 2026-07-05/06 — publish-to-web build session (docs/plans/PUBLISH-TO-WEB-PLAN.md)

| lesson | rule | stored where |
|---|---|---|
| Reusing an Agent `name:` for a fresh dispatch collided (harness auto-suffixed `-2`), and a review-fix message routed to the wrong instance; two agents then raced the same files in the shared worktree. | Give every Agent dispatch a unique task-scoped name; to continue an agent use SendMessage to its exact id, never a same-named fresh spawn. | Memory `feedback_distinct_agent_names_per_dispatch.md` + MEMORY.md. |
| A subagent (reconcile) ran `git rebase` on the shared worktree branch while another subagent (Task 12) was actively working in the same worktree — history moved under the live agent. No corruption only because Task 12 hadn't written yet. | Subagents sharing one worktree must not rewrite history (rebase/amend) without the orchestrator quiescing co-located agents first; after any such rewrite, send the co-located agent a resync message (new HEAD, new baseline) before it commits. | Recorded here; extends `feedback_concurrent_session_shared_git_head.md`. |
| The IDE language server flooded diagnostics with App.svelte exactOptionalPropertyTypes / `Cannot find module '@render/*'` / deploy-flows.svelte.js errors that the studio's CLI `tsc --noEmit` (the CI gate) never reports — because the IDE uses a stricter config and typechecks `.svelte` templates the CLI setup skips. | In this repo, trust `pnpm --filter <pkg> exec tsc --noEmit` (CI parity) over IDE diagnostics; IDE `.svelte`/workspace-package errors are phantoms unless the CLI tsc or `vite build` also fails. Confirm real vs phantom by running the CLI gate, don't chase the IDE. | Recorded here (verification-hygiene rule); relates to `feedback_svelte_rename_grep_callsites.md`. |
| Repeated wave churn dropped legitimately-committed backing code (Task 12's recheckPages, the deploy-flows exports) during rebases — the subagent reports claimed files that the reapplied history no longer contained. | After any rebase/history-rewrite in a multi-agent build, re-derive ground truth from disk (`grep` the actual exports, `git show --stat HEAD`) before dispatching the next task — trust the working tree, not the prior agent's report of what it committed. | Recorded here. |
| A probe→spec→build pipeline (graft divergence → PRFAQ → plan → 14-task execution) ran end to end across many subagents; the git2-in-Tauri feasibility SPIKE before the pack-push task caught the one real unknown early and every later task built on a proven mechanism. | For a build resting on an unproven external mechanism, spike it as Wave-1 (before the task that depends on it), not inline — a cheap yes/no unblocks the whole downstream chain with confidence. | Recorded here; pattern worked, reuse it. |

## 2026-07-16 — go-iiif/iiifpreserve cross-read (4 fixes `42246f1`; non-actionable remainder archived)

Not a tend loop — a verified cross-read of the external `go-iiif`/`iiifpreserve` repo for lessons. 40 candidate lessons → 4 real gaps fixed → 35 confirmed already-done/inapplicable → 1 deferred. Harvested here since nothing left is actionable enough for a live ISSUES.md row.

| lesson | rule | stored where |
|---|---|---|
| The Tauri desktop fs backend re-implements what the browser FSA/OPFS backends give for free and silently dropped **two** guarantees: atomic replace (a crash mid-`writeFile` truncates `library.json`/`manifest.json`) and name containment (raw path-join lets an untrusted slug `..`-escape the root). Both surfaced only by reading the actual backend against go-iiif's `blobstore.go`/`doctor.go` — the go-iiif *design doc* named neither. | The Tauri backend must re-establish atomic-write (same-dir temp → `rename`) and `assertSafeName` validation; any new `TauriFsBridge` method or write path preserves them, and adding a bridge method means updating both implementers (tsc is the gate). | `.claude/rules/tauri-fs-seam.md` (new, priority:high); code in `42246f1`. |
| A WADM `target.source` can be an embedded resource object `{id\|@id}`, not just a string IRI — `String(source)` yielded `"[object Object]"`, missed the `/canvas/` match, and silently dropped a note that belongs here, in `wadm-import.ts` (the module whose stated invariant is "never silently misplace someone's scholarship"). | Extract the source IRI tolerantly (`sourceRef`, mirroring `iiif-import.ts` `idOf`); always test the embedded-object-source shape, not just the string case. | `apps/studio/src/wadm-import.ts` + `wadm-import.test.ts` (`42246f1`). |
| A corrupt authored sidecar (`library.json`/`pending-notes.json`) reads as absent (`JSON.parse` throws → loader returns null/empty), so the next save clobbers it — absent and unparseable are different and must not collapse to one `catch`. | Distinguish absent-vs-unparseable on load; snapshot `{name}.corrupt` before overwriting so a parse failure never masquerades as first-run and destroys authored structure. Defensive here (OPFS writes are atomic → trigger is external, rare). | `apps/studio/src/store.ts` `snapshotIfUnparseable` (`42246f1`). |
| The verified pass killed ~35 of ~40 borrowed "lessons" as already-done or inapplicable; go-iiif's own `DESIGN.md` oversold features its `VERIFIED.md`/`LESSONS.md` admit were unproven or removed. Grounding each lesson against Archie's *actual code* (not the source project's design doc) is exactly what separated 4 real gaps from the noise. | Ground every borrowed external-project "lesson" against actual local code before filing — a design-doc claim is a hypothesis, not a gap; check the source project's own verified-vs-designed gap too. | HARVEST (method rule; extends Issue 2's docs-drift lesson). |
| Automatic retry on transient `429`/`503` honoring `Retry-After` is a real gap (no fetch path retries; `Retry-After` never read) but low-value: every Archie fetch is user-initiated (manual "Try again" suffices), device-flow polling already backs off (`github.rs`), and bulk publish was re-engineered around 429 (single pack push). | Deliberately dropped, NOT filed as a live issue (per "if nothing is actionable, archive"). Revive only if a background/unattended fetch path appears — auto-refreshing remote IIIF or scheduled re-publish. | HARVEST (dropped-with-trigger). |
| No existing ISSUES.md row went terminal this session and the go-iiif fixes were never tracked rows, so there was nothing to collapse — but the prior sessions' many `done` rows (Issues 1–12, 15; Directions 1–3) were never collapsed either: the `## Decided` index still doesn't exist, so ISSUES.md keeps growing. | Standing harvest debt: a future tend harvest should collapse those stored-lesson `done` rows into a `## Decided` index (SHARED.md backlog compaction). Flagged, not acted on here — out of scope (prior-session rows). | HARVEST (flag for next tend run). |

## 2026-07-17 — tend data-integrity re-run (Issues 19–26, Directions 8–9; 5-agent parallel fix round)

| lesson | rule | stored where |
|---|---|---|
| The worst corruption seam (persist.ts) was found independently by two of four parallel explorers walking different beats — convergence from independent walks is a reliable severity/Strength signal, cheaper than adversarial verification. | When parallel explorers overlap on a finding, rank it first — independent convergence is two signals, not one. | Recorded here (method rule). |
| Three recurring silent-drop bugs (sections/readings, note-copy fields, cover/format/originalName) all traced to one convention: hand-spread field carries with no exhaustiveness guard. Fixing the convention (carry.ts sentinels) retired the class at compile time. | Chase the recurring bug class to its shared convention and fix that once — a category fix outranks any instance fix. | `.claude/rules/render-core-data-integrity.md` §3; `model/carry.ts` in code. |
| Multi-file stores need explicit write ordering (content first, index/marker last = commit point) and per-item tolerant reads; mapping corruption to "empty" turns a recoverable tear into permanent loss (the next save orphans everything). | Index/marker is the commit point, written last; corrupt ≠ empty, always surfaced. | `.claude/rules/render-core-data-integrity.md` §1–2; enforced by persist.corruption.test.ts. |
| gen-published's merge preserved exhibits but not the derived images.json — an index left outside a merge step lies silently, and the committed tree proved it (2 of 8 exhibits indexed). | Any merge/regeneration step rebuilds derived indexes from the FULL post-merge set, never from one side's projection. | Regression-locked in image-index.test.ts "merge-preserving bake"; ledgers/BAKE-INDEX.md. |
| The deterministic generation hash (djb2 of tree indexes) made the post-merge regen a one-line diff — byte-stable republish is an oracle: any unexpected diff after regen IS a finding. | Keep publish output deterministic; treat regen diffs as test output. | ledgers/STALENESS.md; recorded here. |
| Characterize-before-fix corrected the backlog's own framing twice (Issue 26's dangling-refs headline was already prevented — the real bug was invisibility; MIRROR row e was not-reachable but exposed a false code-comment premise). | The negative-space matrix's "actual" column exists to correct the issue's framing, not just to queue fixes — fill it before believing your own symptom text. | Recorded here (method rule; the ledgers carry the corrected rows). |
| Parallelizing 8 fix loops across 4 agents worked because grouping was by FILE TERRITORY (orthogonality), not by issue — and one mid-run scope grant (ExhibitView banner) prevented an orphaned handoff between two agents' boundaries. | Group parallel fix agents by disjoint file ownership; when a deliverable falls between territories, grant scope to the running agent whose ledger owns the row rather than spawning a new owner. | Memory `project_parallel_tend_fleet.md`; recorded here. |
| Standing harvest debt from the publish-to-web session (no `## Decided` index; done rows accreting) discharged this session: Issues 1–12/15/19–21/23/24/26 + Directions 1–3/8–9 collapsed to the index. | Collapse terminal rows into `## Decided` the same session their lessons are stored — deferred compaction compounds. | ISSUES.md `## Decided` index (this commit). |
