S8 and design browser verification

Used the run-app skill and root-owned standard stack at http://localhost:5173 after verifying the log showed 543 published files generated and both servers ready. No stack launched or generated library rebuilt by this agent. Browser contexts were fresh.

Results:
- Actual axe color-contrast scan: Studio Library 0 violations; Studio sample overview 0 violations; Viewer gallery at 390×844 0 violations. The original review reported 19 overview failures and 8 mobile gallery failures.
- Found and fixed one additional rendered contrast issue: StorageBar.svelte applied opacity:0.55 to the whole storage chip, multiplying the shared text opacity. Removed whole-chip dimming and its unused transition. Original measured contrast 2.24:1; final sampled page passes.
- Library header visibly reads “Saved · this browser”. Sample overview/editor visibly read “Example · edits aren't kept”. Unified indicator retains read-only/failure precedence.
- Authored overview titles take two actual rendered lines, with identifying opening words readable. Existing filename/identifier suffix policy remains covered by unit tests.
- Mobile summary now measures rendered overflow with ResizeObserver. The shipped 125-character paragraph spans four lines, so the earlier character-count heuristic was inadequate. Collapsed summary is 48px; expanded summary is 96px; actual Read more/Show less interaction and aria-expanded state pass.
- First mobile exhibit title measured y=596.97px, height=29.42px at 390×844. It is entirely in the first 80% of the viewport; earlier review screenshot placed it near y=819px. Document scrollWidth remains 390px. Discovery success with actual visitors is not established by geometry.
- UndoControls now says “Undo is temporary. Reopening restores saved notes.” This matches the existing session overlay/durable log contract. Its legacy undefined color fallbacks now use shared canvas text/surface/border tokens. Actual toolbar screenshot inspected.
- Browser drive reported zero page errors. Undo screenshot was captured while external image canvas was still loading; this check verifies the disclosure, not that media's load completion.

Regression files added:
- apps/studio/e2e/review-contrast.spec.ts: library saved destination; sample overview axe contrast, example copy and two-line authored title geometry.
- apps/viewer/e2e/review-contrast.spec.ts: mobile axe contrast, first exhibit title visibility, no horizontal overflow, summary expansion/collapse.

Focused checks:
- Studio browser specs: 2 passed, using STUDIO_E2E_PORT=5173 against the running front door.
- Viewer browser spec: 1 passed, using VIEWER_E2E_PORT=5173 against the running front door.
- Studio Svelte check: 0 errors, 0 warnings.
- Viewer Svelte check: 0 errors, 0 warnings.
- Earlier targeted unit tests: 44 passed (title presentation, safety state, token ownership).
- git diff --check passed.

Screenshots captured and visually inspected:
/tmp/archie-design-library.png
/tmp/archie-design-overview.png
/tmp/archie-design-mobile.png
/tmp/archie-design-mobile-expanded.png
/tmp/archie-design-undo.png

Browser drive details: /tmp/archie-design-drive.log and /tmp/archie-design-drive.mjs.

Additional changes beyond initial design handoff: Gallery.svelte rendered-overflow measurement; StorageBar.svelte opacity correction; UndoControls.svelte disclosure and shared tokens; the two e2e regression files. Root owns Gallery href/navigation and App wiring; this agent did not edit them.
