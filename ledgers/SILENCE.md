# SILENCE — error-handling census (ISSUES.md Issue 4)

Inventory taken 2026-07-05 against `main` @ `917b274` (working tree also carries an unrelated,
concurrent typecheck-fix pass in progress on `binding.ts`/`publish-flows.svelte.ts` — not this
loop's work; left untouched). Scope: `apps/studio/src/**`, `packages/archie-viewer/src/element.ts`,
`apps/viewer/src/published.ts` (per the issue's Run-it). Classify every site before fixing anything.

Two ISSUES.md-seeded sites turned out to be **misclassified by the automated anti-pattern scan**
that originally flagged them — recorded below with the correction and why. The real highest-severity
sites (rows 1–2) were **not** in the seed list at all; they surfaced from tracing what actually calls
each persistence function, not from grepping for the word "catch".

## Persistence path (studio) — blast radius first

| site | trigger | disposal today | should be | fix commit | forced check |
|---|---|---|---|---|---|
| `App.svelte:663,669` `void persistPending()` → `store.ts savePendingNotes` (no catch anywhere in the chain) | OPFS write failure (quota exceeded, permission revoked) while staging a CSV-imported pending note or clearing the sidecar on project replace | **unhandled** — an unhandled promise rejection; the pending note silently never reaches disk, user sees nothing | surfaced — catch, log, and set a user-visible error (route through `save-queue.svelte.ts`'s `enqueueSave`, the module's own stated contract: "every persist path routes through here") | `1228f61` | verified: studio suite green (148/148); throwaway test against the real `enqueueSave` confirmed a simulated write failure sets `saveStatus.error` instead of vanishing |
| `App.svelte:575` `void flows.addFiles(e.dataTransfer?.files ?? null)` (drag-drop) and `App.svelte:1402` `void flows.addFiles(el.files).then(() => (el.value = ""))` (file picker) → `ingest-flows.ts addObjectFromFile` → `saveAssetFile`/`saveOriginalFile` (no try/catch anywhere in the call chain) | OPFS write failure on a single-file image/audio/video import (large media import is exactly the quota-pressure case) | **unhandled** — unhandled promise rejection; `.then()` at the picker site only handles success (input value reset also silently skipped on failure), no `.catch()` at either call site. Inconsistent with the sibling folder-import path (`newExhibitFromFolder`), which does catch and tally per-file failures | surfaced — add `.catch()` at both call sites (console.error + user-visible message via the toast layer, `e45f38b`) | `1228f61` | verified: studio suite green (148/148); a standalone Node check confirmed `.catch()` attached directly to the promise intercepts the rejection where the old `.then()`-only shape would not |
| `apps/studio/src/binding.ts` `saveZipToDisk`'s outer `catch (e) { await writable.abort().catch(() => {}); throw e; }` (ISSUES.md's originally-cited "binding.ts:95") | The FSA `writable.abort()` cleanup call itself throws, during handling of an already-failed save | **swallowed** — the *primary* save failure IS surfaced (rethrown → `binding-store.svelte.ts:112-115` → `s.error`, "the recovery card renders this"); only the *secondary* cleanup failure (abort failing, possibly leaving a stuck partial file) is invisible | logged — `console.warn` the secondary failure so a stuck partial file is at least debuggable; the primary surface is already correct, no change there | `a346b33` | verified: binding suite green (10/10); a standalone check confirmed the primary error still propagates while the secondary abort failure now reaches `console.warn` |
| `apps/studio/src/save-queue.svelte.ts:47` `tails.set(key, run.catch(() => {}))` (ISSUES.md's originally-cited bare catch-all) | **Correction, not a bug.** This is plumbing, not error handling: it keeps the per-key promise *chain* from being permanently poisoned by a rejected job, so the *next* enqueued save for the same key still runs. The actual error handling is the sibling `return run.then(onSuccess, onError)` two lines below (44-62), where `onError` does `console.error` **and** sets `s.errors[key]`, consumed by `saveStatus.error` in the App chrome / LibraryHome project bar. The original anti-pattern-report.txt flagged this by syntax alone (`.catch(() => {})` pattern-matches "bare catch-all") without reading the surrounding function | already surfaced — verified correct, by design | n/a — no fix | verified 2026-07-05: read the full function, confirmed the returned promise's `.then(onSuccess, onError)` is what every caller actually awaits |
| `apps/studio/src/store.ts` read-path OPFS degrades: `readAssetFile:206`, `readOriginalBytes:291-299`, `readThumbBytes` (mirrors `readAssetBlob`), `readAssetSize:220-226` — four near-identical `catch { return null / 0 }` sites feeding the publish reader (`getAsset`/`getOriginal`/`getThumbnail`) | OPFS read failure (corrupted handle, permission revoked mid-session) is indistinguishable from "this asset was legitimately never saved" — both silently produce the same null/0 | swallowed (by design for the "never saved" case; ambiguous for the "failed" case) — `readOriginalBytes` specifically gates the opt-in "include source for citation" publish feature, so a real read failure here means a citation-quality original silently drops out of a publish with zero indication | by-design, park — genuinely low severity (an opt-in, best-effort read path) and disentangling "absent" from "failed" would need a filesystem-level distinction none of the three backends (OPFS/zip/memory) currently expose; not worth the surface area for this loop | n/a | n/a |
| `apps/studio/src/store.ts` `loadLibraryMeta:56`, `loadPendingNotes:95` catches → null/`{}` | OPFS read failure on the two structural sidecars, comments read "not yet written (first run)" | by-design for the common case; same absent-vs-failed ambiguity as above, but here a *false* "not yet written" on an EXISTING corrupted `library.json` would present as data loss (the author's structure reads back as empty) | by-design, park — same reasoning as above; flagging the ambiguity is the useful record, not a fix this loop should force | n/a | n/a |
| `apps/studio/src/store.ts` `clearExhibitAnnotations:252`, `exhibitHasAnnotations:275` | OPFS directory-not-found on housekeeping reads, comments read "fine" / "nothing stored for this exhibit" | by-design — genuinely low-stakes (a missing directory on a probe read is the expected empty case) | by-design, no change | n/a | n/a |
| `apps/studio/src/handles-db.ts` (`putHandle:39`, `getHandle:48`, `deleteHandle:54`, `queryPermission:67`, `requestPermission:73`) | IndexedDB unavailable / private-mode, comments read "unsupported / private mode" | by-design — losing a remembered folder HANDLE just means the user re-picks the folder next time (re-prompt, not data loss) | by-design, no change | n/a | n/a |
| `apps/studio/src/binding.ts` (`loadRecents:116`, `saveRecents:122`, `loadLastBinding:136`, `saveLastBinding:145`) | `localStorage` unavailable (private mode), comments read "storage unavailable" | by-design — UX nicety only (recent-projects list, remembered binding descriptor), never the annotation/library data itself | by-design, no change | n/a | n/a |
| `apps/studio/src/App.svelte` (`loadIdentity:59`, `firstAddSeen/markFirstAddSeen:69-70`, `loadAsideW/loadAsideCollapsed:440-441`, aside-resize persist:448, `loadPanel:457`, panel persist:461, `loadNotePinned:479`, pinned persist:481) | `localStorage` unavailable, every site carries an explicit "harmless"/"private mode" comment | by-design — UI-preference state only | by-design, no change | n/a | n/a |

## Ingest flows (studio)

| site | trigger | disposal today | should be | fix commit | forced check |
|---|---|---|---|---|---|
| `apps/studio/src/ingest-flows.ts:442-449` `openZip`'s catch (ISSUES.md's cited site) | Any failure opening a dropped `.archie.zip`: a zip-bomb cap breach (`ZIP_LIMITS`, three distinct messages in `fs/zip.ts`), a corrupt/non-Archie zip, or (if ever wired) a schema-version mismatch (`NotAnArchieLibraryError`, whose own doc comment says its message is meant to be "surfaced verbatim") | surfaced, but **generic** — every cause collapses to one string, discarding a message the thrown `Error` already carries. Confirmed by comparison: `apps/viewer/src/published.ts`'s `openError()` explicitly documents itself as mirroring this exact function, but *does* pass `e.message` through for `instanceof Error` — the two "twin" implementations (Issue 5) have already drifted on this exact behavior | surfaced, cause-specific — pass `e.message` through when `e instanceof Error` (same one-line pattern as the viewer's already-correct `openError`), generic fallback only for a non-Error throw | `a587471` | verified: studio suite green (148/148); a forced check with a malformed zip (real `ZipFilesystem.fromZip`) confirmed the thrown message ("invalid zip data") now reaches `ctx.alert` instead of the generic line |
| `apps/studio/src/ingest-flows.ts:310-321` `newExhibitFromManifest`'s two catches (fetch-failure, manifest-parse-failure) | Unreachable IIIF endpoint / malformed manifest | surfaced — distinct alert messages per failure type already | already adequate | n/a | n/a |
| `apps/studio/src/ingest-flows.ts:289` per-file catch inside `newExhibitFromFolder`'s import loop (`failed++`) | One file in a folder import throws (corrupt image, OPFS write failure) | surfaced, but generic — tallied into "N couldn't be added" with no per-file reason | acceptable for this loop — the batch-summary pattern is a reasonable UX tradeoff for a multi-file import; sharpening it is folded into Issue 7 (ingest negative-space matrix), not duplicated here | n/a | n/a |
| `apps/studio/src/AvEditor.svelte:247` `readPeaks(...).catch(() => null)` | Waveform peak-cache read miss/corruption | by-design — comment explains the fallback (re-decode from scratch) | by-design, no change | n/a | n/a |
| `apps/studio/src/AvEditor.svelte:274` `savePeaks` write catch → `console.warn` | Peak-cache write failure (pure perf optimization, never published) | logged-and-lost, but appropriately low severity — losing the cache costs one re-decode next open, nothing else | acceptable, no change | n/a | n/a |
| `apps/studio/src/publish-flows.svelte.ts` DZI-tiling / remote-refetch degrades (~lines 64-108) | Non-decodable raster, remote host down/CORS during publish-time re-tiling | by-design — every site carries an explanatory comment (leaves the object pointing at the single source instead of a tiled one) | by-design, no change | n/a | n/a |
| `apps/studio/src/publish-flows.svelte.ts:194-200` save-to-disk `AbortError`-vs-rethrow | User cancels the native save-file picker vs. a real save failure | correct — `AbortError` returns `false` (picker cancelled, not an error), everything else rethrows to the caller's surfaced path | already correct | n/a | n/a |
| `apps/studio/src/PublishDialog.svelte` clipboard-copy catches (`copyShareLink:59`, `copyEmbed:81`, `copyWc:86`) | `navigator.clipboard.writeText` permission denied | by-design — comment notes the link/snippet is still visible and selectable in the dialog even if auto-copy fails | by-design, no change | n/a | n/a |

## archie-viewer element (`packages/archie-viewer/src/element.ts`)

| site | trigger | disposal today | should be | fix commit | forced check |
|---|---|---|---|---|---|
| `element.ts:247-261` `#openExhibit`'s catch (ISSUES.md's cited site, lines 254-261) | Any failure reading/resolving an exhibit after the library is already open (corrupt manifest, broken cite-ladder target) | **logged-and-lost** — `console.error` only (invisible to anyone but a developer with devtools open), the visible result is a bare **empty exhibit shell** (title = slug, zero objects) with no indication anything went wrong. A visitor on someone's embedded course page just sees a blank gallery | surfaced — extend the exhibit view state to carry an optional error message (mirroring the sibling `{kind:"empty", error}` pattern already used at lines 151/180 for the top-level open failure) and render it instead of a bare empty shell | `893284e` | verified: archie-viewer suite green (98/98), package typecheck clean; a forced check with a genuinely corrupted exhibit `manifest.json` confirmed the real parse error now renders visibly in the shadow DOM (`.err`) instead of a blank grid |
| `element.ts:203-215` `resolveContentState`'s catch (IIIF Content-State address resolution) | Malformed/foreign `iiif-content` attribute value | **swallowed, zero log** — comment explicitly says "any unexpected failure in the resolve degrades upward, never throws out" (a deliberate, reasonable degrade), but there is no `console.warn` at all, unlike every sibling catch in this file | logged — add one `console.warn` so a malformed Content-State address is at least debuggable; the degrade-to-gallery behavior itself is correct and unchanged | `893284e` | verified: archie-viewer suite green (98/98) alongside the row above |
| `element.ts:141-154` top-level `src=` open failure, `element.ts:170-184` file-open failure | Library open failure (bad URL, corrupt/oversized zip, marker reject) | surfaced — `{kind:"empty", error: message}`, rendered by the empty-state view | already correct | n/a | n/a |
| `element.ts:308-323` object-mount failure, `element.ts:344-354` AV-mount failure | OSD/AV mount throws (offline-blocked, load error) | surfaced — `host.innerHTML` replaced with a visible `.notice` message | already correct | n/a | n/a |

## `apps/viewer/src/published.ts`

| site | trigger | disposal today | should be | fix commit | forced check |
|---|---|---|---|---|---|
| `initLiveSource:75-99`, `probeViewerMode:129-150`, `fetchJson:255-267`, `fetchJsonOptional:271-281`, `loadGallery:230-244`, `openZipBytes`/`openError:159-182` | OPFS probe miss, HTTP 404/5xx, malformed JSON, network failure — the full ingest surface for the hosted+live+portable viewer | **all by-design, and unusually well-instrumented** — every site carries an explanatory comment stating the exact intended distinction (e.g. `modeFromProbe`'s doc comment: "a 404 is the ONLY 'portable shell' signal; every other failure is an error", `fetchJsonOptional`'s comment: "still degrade to null... but surface it so a hiccup isn't silently misread as 'no data'"); each degrade pairs with a `console.info`/`console.warn`/`console.error` matched to its severity | already adequate — this file is the reference pattern the rest of the audit was measured against, not a source of findings | n/a | n/a — no site here needed a forced-failure check; nothing changed |

## Notes

- Row ordering follows blast radius per the issue's Loop: persistence (studio) first, ingest second,
  viewer-embed degrades last.
- Two rows above **correct** ISSUES.md's own seed list (`save-queue.svelte.ts:47` was a false positive;
  `store.ts:298` is `readOriginalBytes`, a read path, not the write path its "did my work save?" framing
  implied). Both corrections are logged in-row with the evidence, not silently dropped.
- The two highest-severity rows (`persistPending`, `addFiles`) were found by tracing callers, not by
  grepping for `catch` — neither site contains the word `catch` at all, since the bug *is* the missing
  catch. A pure keyword sweep (including the one that produced the now-deleted `anti-pattern-report.txt`,
  ISSUES.md Issue 3) cannot find this class of defect.
**Done 2026-07-05.** Every row reads surfaced/reported (five newly fixed: `1228f61` ×2, `a346b33`,
`a587471`, `893284e`) or by-design/already-correct with its reason cited in-row. Every forced failure
(persistPending's simulated write throw, the `.catch()` shape check, binding.ts's simulated primary+
secondary failure pair, a malformed real zip, a genuinely corrupted exhibit manifest) was observed
landing exactly where its row says.

- Out-of-scope, not fixed here: `ingest-flows.ts`'s `openZip` skips `validateArchieMarker` entirely
  (`loadLibrary` in `render-core/publish/site.ts` never calls it) — the viewer's twin path does call it.
  This is evidence for **ISSUES.md Issue 5** (the duplicated untrusted-zip open path), not this loop;
  logged here for cross-reference, not actioned.
- Operational note: the concurrent Issue-1 session shares this working directory and, between the
  inventory commit and the row-1/2 fix, ran `git checkout ci/wire-checks` on it — a repo-wide HEAD
  change, not session-local. The row-1/2 fix commit therefore landed on `ci/wire-checks` first
  (`a8aa23e`) and was cherry-picked onto `main` as `1228f61`, which is the hash recorded above.
  `ci/wire-checks` still carries the original `a8aa23e` as an extra, harmless duplicate — left alone
  deliberately (not this loop's branch to rewrite).
